Status pill for the top-bar model selector — a live status dot plus a label.

```jsx
<Pill status="ok">DeepSeek · 本地优先</Pill>
<Pill status="run">Claude · 执行中</Pill>
<Pill status="idle">离线</Pill>
```
Status colors the dot only: `ok | run | warn | err | idle`. Keep the label to a model name + short qualifier.
