Capability / filter pill. Powers the welcome capability row and selectable filters.

```jsx
<Chip selected icon="🔒">本地优先</Chip>
<Chip>不锁平台</Chip>
<Chip icon="⚡" onClick={...}>真执行</Chip>
```

Pass `selected` for the on state, `onClick` to make it interactive. Pill radius always.
