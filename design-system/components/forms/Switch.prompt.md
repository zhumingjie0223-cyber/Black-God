iOS-style toggle with a chrome track when on. For privacy guardrails and local-first switches.

```jsx
<Switch checked={local} onChange={setLocal} />
```
Controlled only — pass `checked` + `onChange`.
