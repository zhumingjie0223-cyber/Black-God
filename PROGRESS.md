
## 补充需求（权哥 2026-07-31 追加，重要）
7. **API 界面极简：表面只留一个输入框**（填/粘贴用），其余所有技术字段
   （服务器地址、模型名、token 等）全部隐藏("掩瞒")，普通用户看不到那一堆。
8. **"用哪个模型"必须能选**：给一个模型选择器（下拉/选项卡），让用户能挑用哪个模型，
   不能是"没有选择题"（现状=没得选）。
   → 综合 2+7+8 的最终形态：**一个输入框 + 一个模型选择器，其余字段全自动补全/隐藏**。
