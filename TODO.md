# TODO — 记忆深化:显式事实记忆(权哥 2026-07-19)

方向:主人明说"记住/叫我/我的X是Y/以后都…"→ 神枢立刻抓成"驻留事实",每次回话都带着,
永不等情节溢出、永不遗忘。这是「记忆深化」方向的又一层(继情节巩固→长期记忆之后)。

## 步骤
- [x] 后端 extractFacts(text):正则抓 记住/叫我(转称呼)/我的X是Y/以后都… 四类,去重、封顶 50。
- [x] 后端 summarizeFacts(facts):注入动态上下文最前(【主人交代·须始终记牢】),优先级最高。
- [x] 临界段捕获:每轮对话抓事实、去重并入 soul.facts(封顶 50),不等溢出。
- [x] getInner 暴露「事实」字段(最近 20 条),供意识面板展示。
- [x] UI:记忆面板新增「神枢牢记的事·你交代过的」卡片(置顶),演示态填样例。
- [x] selftest 加 9 条事实记忆用例(抓取/转称呼/键值/以后/闲聊不抓/空/去重/摘要)。
- [x] build + selftest(174 通过) + node --check + sync-ui --check 双副本一致。
- [ ] 中文提交 → 力推分支 → 建草稿 PR → 合并自动部署。

## 红线
- 纯逻辑,不碰 /api/confirm、不碰词根表、不碰身份归一化;向后兼容(无 facts 字段安全降级)。
