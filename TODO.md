# TODO — 六个悬置 PR 一次性清账（权哥 2026-08-17 拍板 A+B+C 一起办）

来源：权哥「三个可以一起吗」——A（救 #90/#86）+ B（关 #79/#50）+ C（啃 #87）三档一起干。
前置：已实测全部 6 个 PR 的真实冲突面（补全浅克隆后逐个 merge-tree 试合并）。

## B 档 · 先清噪音 —— ✅ 已完成

- [x] B1. 关 #79（CF 机器人自动配置）—— 已附中文关闭说明并关闭。
      理由：会把根 `wrangler.jsonc` 加回来，即 7 月主动删掉的 `blackgod88` 影子 worker 配置。
- [x] B2. 关 #50（会话时间线 + shuyu_v2）—— 已附中文关闭说明并关闭。
      理由：落后 328 提交，改的 `shuyu_v2/` 已从 main 移除；文档价值已被后续归档覆盖。

## A 档 · 捡易得 —— ✅ 已完成

- [x] A1. #90 视觉层次与触感升级 —— 已推送 `96316bb`。
      唯一冲突 `nexus_do.mjs` 是构建产物，重跑 build 解决；`index.html` 自动合并成功
      （696,665 → 698,786 字节）。core.mjs 与 main 逐字节一致，未动核心逻辑。
      验证：selftest 236/236 · 单测 65/65 · sync-ui 一致 · check-sync 引擎全同步。
- [x] A2. #86 六轴强化方案 —— 已推送 `2b6f8e1`。
      唯一冲突 `docs/archive/wrangler.toml.2026-08-09.bak`，按「先备份再删除」铁律保留 main 的归档版。
      核实未夹带部署风险（wrangler.toml 未复活 / 根 wrangler.jsonc 未加回 / 工作流仍 5 条）。
      验证：selftest 236/236 · 单测 75/75 · 枢语 Node 33/33 + Python 9/9 · 双校验通过。
      ⚠ 已在 PR 上标出一处需权哥拍板：分支给 `config/wrangler.jsonc` 加了 build hook
      去**修好** blackgod88 的红灯，而 LAUNCH_CHECKLIST 原定方向是**关掉**它——方向相反，我没替你选。

## C 档 · #87 —— ⏸ 已完成技术勘察，卡在权哥的设计决策上（未推送任何东西）

- [x] C1. 合 main 后确认三处真冲突：`index.html`(2 块) / `nexus_do.core.mjs`(4 块) / `nexus_do.mjs`(产物)
- [x] C2. `nexus_do.mjs` 走重建解决 ✓
- [x] C3. `index.html` 两块冲突均取 main 侧（分支侧是旧版硬编码色值，main 侧是设计令牌化
      且带 iOS 灵动岛安全区修复，按设计系统铁律「沿用令牌不硬编码」判定）
- [x] C4. `core.mjs` 四块：双方实现全部保留不删功能，API 白名单取并集（40 条）；
      修掉两处因拼接导致的花括号失衡，`node --check` 通过
- [x] C5. 验证：build 通过 · selftest 236/236
- [x] C6. **勘察出必须权哥拍板的两件事，按红线停手，已 `git merge --abort` 丢弃合并、未推送**

### C 档勘察结论（三条硬事实）

1. **两套并行的 Telegram 入站实现**（这是真分叉，不是格式冲突）
   - main：`/tg` → `handleTelegramWebhook`（**当前线上部署的就是这套**）
   - #87：`/tg/webhook` → `handleTgWebhook`，另带 `/tg/setup` 一键注册 + 心跳自愈重注册
     （`_pubOrigin`/`public_origin` 落盘公网地址供自愈用）
   - 合并后两套都在、都能跑，但 #87 的心跳自愈会**主动把 Telegram 的 webhook 重新指向 `/tg/webhook`**，
     等于静默把线上 TG 处理从 main 那套换成 #87 那套。谁对我无法验证——
     Telegram 当前注册的 webhook 地址是服务端状态，仓库里看不到。**必须权哥定。**

2. **Logo 资产换了**：main 是 253KB 的 PNG，#87 换成 10KB 的 WebP，**压了 96%**。
   我把两张图都解出来看过：**是同一个神字标**（思涵身形嵌在右半边、石墨黑金 + 玉绿辉光、圆角方形），
   #87 版裁切更紧、外发光弱一些。属优化不属换标，但按品牌铁律仍请权哥亲眼过一下。

3. **UI 是两套竞品设计**：#87 引入 22 处 `nx2-cap` 能力按钮面板（main 没有），
   同时会顶掉 main 侧约 66 行 / 5.7KB 的样式与结构
   （`lock-orb` 登录门光晕、深色主题 lockgate 渐变、`hz-coord`、`pp-i`、`sheet-sub`、`field label` 等）。
   主结构没丢（Tab 导航 4 处两边都在、灵动岛修复保住了），但这是**设计取舍不是技术冲突**，
   得看渲染效果拍板，不该由我替权哥决定。

### 顺手查出的文档漂移（不影响合并）

`CLAUDE.md` 设计系统铁律写主强调色是 `--cy-hi:#3DDC84`，
**实际 main 里是 `--cy-hi:#4FE096`**。铁律文档又落后于实现了，建议单独修一下。

## 收尾

- [ ] D1. 等权哥对 C 档三条拍板后继续
- [ ] D2. 全部收口后归档 `docs/done/`

## 红线（本次严格遵守）

- 只推了权哥明确授权的 #90 / #86 两个分支；#87 一行未推。
- `/api/confirm` 二次确认（实为 `?confirm=1` 闸门）、词根表、生产 `wrangler.jsonc`、玉绿令牌：未动。
- 每个推送前都跑过 build + selftest 全绿，没推过红的。
- C 档遇到品牌/设计/线上接线的取舍，**停下问权哥**，没硬猜。

---

## 第二轮（权哥 2026-08-17「一一啃下来」）

- [x] E1. 啃 #84 品牌图标 v2 + 联网检索多源兜底（8 文件冲突，含二进制图标）
- [x] E2. 修 CLAUDE.md 主强调色漂移（#3DDC84 → 实际 #4FE096）
- [ ] E3. 回头啃 #87：按「main 是权威基线 + 不删分支独有能力 + 不静默改线上接线」三原则给出保守解
- [ ] E4. 全部收口后归档 docs/done/
