	__minis__
	minis_url
	var/minis
                            __minis__.resolve(JSON.stringify(__v__));
                            __minis__.resolve(String(__v__));
                            __minis__.saveBlobDownload(reader.result, 
                        __minis__.reject(e.message || String(e));
                        __minis__.resolve('null');
                        __minis__.resolve(JSON.stringify(__v__));
                        __minis__.resolve(String(__v__));
                        reader.onerror = function() { __minis__.blobDownloadError('FileReader error'); };
                      --input msgs.json --output /var/minis/workspace/out.txt
                    .catch(function(e) { __minis__.blobDownloadError(String(e)); });
                    __minis__.reject(e && e.message ? e.message : String(e));
                   and referenced via image_path + minis_url.
    <path>/var/minis/skills/
    fetched_minis_url minis://browser/<filename> for the download
    fetched_path      Linux path of the persisted download under /var/minis/browser/
    iOS `data.path = /var/minis/offloads/...` exactly.
    image_path        Linux path of the persisted JPEG under /var/minis/browser/
    minis_url         minis://browser/<filename> 
  (non-minis scheme, no fallback)
  - Audio:  ![song](minis://attachments/song.mp3)     
  - Images: ![chart](minis://attachments/chart.png)   
  - Video:  ![clip](minis://attachments/clip.mp4)     
  /var/minis/attachments/ 
  /var/minis/attachments/file.png
  /var/minis/browser/     
  /var/minis/memory/GLOBAL.md    
  /var/minis/memory/YYYY-MM-DD.md 
  /var/minis/mounts/<name>/      
  /var/minis/offloads/    
  /var/minis/shared/      
  /var/minis/shared/project/f.txt
  /var/minis/workspace/   
  /var/minis/workspace/data.csv
  android-speech transcribe --source /var/minis/attachments/meeting.m4a
  echo 'What is 2+2?' | minis-model-use run --model gpt-4o
  minis-browser-use --help
  minis-browser-use --json '<json>'
  minis-browser-use --json '{"action":"navigate","url":"https://x.com"}'
  minis-browser-use <action> [options]
  minis-browser-use click --selector '.btn-primary'
  minis-browser-use execute_js --script 'return document.title'
  minis-browser-use navigate --url https://example.com
  minis-browser-use screenshot
  minis-browser-use type --selector 'input[name=q]' --text 'hello'
  minis-config <subcommand> [args]
  minis-model-use list
  minis-model-use list --modality image_input
  minis-model-use list [--provider <name>] [--modality <mod>]
  minis-model-use run --model <id_or_name> [--provider <label_or_id>]
  minis-model-use run --model claude-sonnet-4-6 --input /var/minis/workspace/prompt.json
  minis-model-use run --model deepseek-v4-flash --provider deepseek --input msgs.json   # equivalent
  minis-model-use run --model deepseek/deepseek-v4-flash --input msgs.json   # qualified form
  minis-model-use run --model gemini-2.5-flash --system 'You are a poet' \
  minis-model-use search <query> [--provider <name>] [--modality <mod>]
  minis-model-use search gemini
  minis-sessions-cli <command> [options]
  minis-sessions-cli list
  minis-sessions-cli list --keywords python flask
  minis-sessions-cli list --limit 10
  minis-sessions-cli list --start 2025-01-01 --end 2025-03-31
  minis-sessions-cli messages --id <session_id>
  minis-sessions-cli messages --id <session_id> --full
  minis-sessions-cli messages --id <session_id> --offset 20 --limit 10
  minis-sessions-cli search --keywords "API error" --limit 20
  minis-sessions-cli search --keywords deploy --ids abc123,def456
  minis://attachments/file.png  
  minis://shared/project/f.txt  
  minis://workspace/data.csv    
 Config Changes. If the call returns `permission_denied`, the user has disabled minis-config in [Settings 
 Config Changes](minis://settings/logs?tab=config-audit).
 HTML pages and all sub-resources (JS, CSS, images, fonts, etc.) referenced via minis:// absolute URLs or relative paths resolve correctly within the current session. When building multi-file web projects, use file_write to create files in the same directory (e.g. /var/minis/workspace/myapp/), then reference sub-resources with relative paths in HTML (e.g. <link href="style.css">, <script src="app.js">, <img src="logo.png">). The browser resolves relative paths against the minis:// base URL automatically. Cross-directory references also work with absolute minis:// URLs (e.g. <img src="minis://attachments/photo.png"> from a workspace HTML page). Navigate to the entry HTML to preview, e.g. minis://workspace/myapp/index.html.
 Lcom/openminis/app/MainActivity;
 Media files (images, audio, video). Display inline with ![desc](minis://attachments/filename).
 Memory](minis://settings/memory) to re-enable it.
 Permissions](minis://settings/permissions), then ask me again. Or change the setting yourself directly through the relevant Settings screen.
 Permissions](minis://settings/permissions).
 Permissions](minis://settings/permissions); relay that message and don't retry. You CAN add new providers and write their `apiKey` (literal string OR a `$$ENV_VAR` reference to copy from an env var at write time), but `get` never echoes API keys / OAuth tokens / env var values back 
 Working files (scripts, data, configs). Link with [name](minis://workspace/filename).
 X" or want to point them at a specific setting, prefer a Markdown link `[Label](minis://settings/<path>)` over plain prose. Available paths: providers (list), providers/<instanceId> (one provider), model-groups (incl. Agent Loop), model-groups/<groupId>, usage (token usage), skills, memory, storage, shared-folders (Shared Folders: /var/minis/{shared,skills,memory}), mount-external (Mount External Folders), logs, appearance, background, about, permissions, environments[?create_key=K&create_value=V[&create_note=N]], rootfs (also reachable as mirrors). Unknown paths fall back to Settings home, but prefer the exact path so users land where they want. minis:// URLs are app-internal 
 all tool permissions cleared, minis-config switch reset to default
 built-in WebKit preview) and chat-resource file paths under /var/minis/** (
 built-in file preview, routed by extension: images to the image viewer, .md to markdown preview, .html to HTML preview, .pdf/office docs to QuickLook, audio/video to the media player, else share sheet). Examples: minis-open https://example.com, minis-open /var/minis/workspace/report.md, minis-open /var/minis/attachments/chart.png. Prefer this over android-open for anything that can be previewed in-app so the user doesn't lose conversation context. Use android-open for non-web schemes (tel:, mailto:, geo:, intent:, etc.) or when the user explicitly wants the system handler.
 com.openminis.app.scheduled.FIRE
 for everything else, use shell_execute. Examples: [Open Terminal](minis://open_terminal), [Login to SSH](minis://open_terminal?init_command=ssh%20user%40host).
 it is already encoded. If you construct a minis:// URL manually, percent-encode the filename (e.g. %E4%B8%AD%E6%96%87 for non-ASCII characters).
 list/cancel are not supported (no system query API); tell the user to manage them from the Clock app's Alarms/Timers tabs (or `android-alarm open` / minis://views/alarm).
 minis://workspace/
 prompts that run automatically at a chosen time. `minis-scheduled create --time HH:MM --prompt "..." [--label L] [--repeat once|daily|weekdays|custom --days mon,tue,...] [--target new|follow-up|rerun --session <id> --message <id>] [--model <modelId>] [--start YYYY-MM-DD] [--end YYYY-MM-DD]` schedules it; `list` shows existing tasks (with nextTriggerMs and run history), `delete --id <taskId>`, `enable`/`disable --id <taskId>`, and `run --id <taskId>` fires one immediately. Target modes: `new` runs the prompt in a fresh chat; `follow-up` appends the prompt to an existing chat (--session); `rerun` re-runs an existing chat (--session) from a chosen user message (--message). Use this when the user asks to "remind me / do X every morning / run this later / schedule a task". Run --help for full usage.
 raw cookie values are NOT included in the tool response. To reuse cookies in shell commands: `. /var/minis/offloads/env_cookies_xxx.sh && command`. You may define alias variables when needed. Use set_cookies to write cookies into the current page's cookie store via the native cookie store (so even HttpOnly cookies, which JS cannot set, land). Pass a 'cookies' array of objects, each with name + value (required) and optional domain (defaults to the current page host), path (defaults to '/'), secure, http_only, and expires (Unix timestamp in seconds; omit for a session cookie). Use wait_for_dom_stable to wait until the page DOM stops changing (useful after navigation or interactions that trigger async data loading 
 screenshots are persisted to /var/minis/browser/
 they are NOT web URLs. Do NOT pass minis:// action URLs (open_terminal, views, settings) to browser_use 
 those are app deep links, use Markdown links in chat instead. However, minis:// resource URLs CAN be opened in browser_use with navigate. All directories under /var/minis/ are accessible: workspace, attachments, offloads, shared, etc. The built-in browser fully supports minis:// 
 those are app deep links, use Markdown links in chat instead. The browser supports both web URLs and minis:// resource URLs. Use minis:// URLs to preview session files (e.g. navigate to minis://workspace/index.html). Sub-resources (JS, CSS, images, fonts) referenced via minis:// absolute paths or relative paths within HTML pages resolve correctly. Use navigate to open URLs, screenshot to see the page (returns an image), click/type to interact with elements, get_text/get_readable to extract content, scroll to navigate long pages, scroll_and_collect to scroll through infinite-scroll/virtual-rendered pages (like Twitter/X timelines) and accumulate unique content items across scroll positions in a single call, find_elements to discover interactive elements, get_page_info for page metadata, get_backbone to get a structural overview of the page DOM as a simplified tree, fetch to download files/resources using the page's session (returns metadata and a minis:// URL), new_tab to open an additional tab, close_tab to close a tab, and list_tabs to see all open tabs. Use set_viewport with viewport_width + viewport_height to override the viewport for the current session (e.g. before screenshotting a 1920
 those reads return `permission_denied` by design. OAuth tokens and env var values are not settable via this tool; for an env var, point the user at [Set ENV_NAME](minis://settings/environments?create_key=ENV_NAME&create_value=) so they enter the value themselves.
 use the ![desc](minis://...) image syntax for ALL of images, audio, AND video. The same ![]() syntax renders an inline audio player or video player, not just images:
!/var/minis/attachments/spillover/
!mailto:dev@openminis.app?subject=
!minis-config/mounted-folders.json
!minis-scheduled: no task with id=
" url="minis://
# available). Try: minis-config get 
$com.openminis.app.STOP_AGENT_SERVICE
$com.openminis.app.action.OPEN_WEBAPP
%Lcom/openminis/app/sandbox/PtyBridge;
%com.openminis.app.data.model.LLMModel
%com.openminis.app.data.model.LLMUsage
%com.openminis.app.data.model.MediaRef
%minis-model-use: unknown subcommand '
%minis-scheduled: unknown subcommand '
&com.openminis.app.MainActivityIconAuto
&com.openminis.app.MainActivityIconDark
&minis://settings/logs?tab=config-audit
'Lcom/openminis/app/data/db/AppDatabase;
'com.openminis.app.MainActivityIconLight
'com.openminis.app.data.model.ModelEntry
'com.openminis.app.data.model.ModelGroup
(Lorg/acra/config/ReportingAdministrator;
) is not supported by minis-model-use on Android yet 
) to /var/minis/workspace/
)Lcom/openminis/app/offload/AlarmReceiver;
)Lcom/openminis/app/webapp/WebAppActivity;
)Whole groups exposed via minis-model-use.
)com.openminis.app.data.model.ProviderType
*com.openminis.app.data.model.ThinkingLevel
*minis-model-use run: cannot read --input '
+](minis://settings/environments?create_key=
+com.openminis.app.data.model.ModelOverrides
+com.openminis.app.data.model.ProviderConfig
, items). To get more, use: minis-config get 
, minisSize=
,Lcom/openminis/app/crash/NativeCrashHandler;
,Lcom/openminis/app/data/db/AppDatabase_Impl;
,Lcom/openminis/app/data/db/ProviderDatabase;
,com.openminis.app.data.model.RoutingStrategy
- File search: when looking for user files, do NOT scan the whole filesystem. Search under /var/minis/ first (workspace/attachments/shared for the current session, mounts/* for user-provided external folders). Only widen the scope if the file is clearly not under /var/minis/.
- GLOBAL.md (/var/minis/memory/GLOBAL.md) stores persistent preferences, settings, and general-purpose conventions. To read it, use file_read (NOT memory_get). To update it, use file_read first then file_edit. If GLOBAL.md does not exist yet, use file_write to create it directly.
- When a skill or task requires an environment variable that is not set, tell the user which variable is missing and provide a tappable deep link to create it: [Set ENV_NAME](minis://settings/environments?create_key=ENV_NAME&create_value=) 
- minis-config: Read or change Minis settings programmatically. Run `minis-config --help` for subcommands and `minis-config topic-help <topic>` for details on a specific area. For array-valued fields (e.g. `models`, `groups`, `envvars`, `defaults.agentLoopEntries`) the `get` subcommand accepts `--filter <keywords>` (whitespace-AND, case-insensitive substring match against each element's JSON) and `--page <N> --page-size <N>` (default 20, max 100) 
- minis-model-use models available: 
- minis-model-use: Invoke other LLM models pre-configured by the user. Use `minis-model-use list` to see them (includes each model's modality capabilities like image_output, audio_output, etc.), `minis-model-use search <query>` to filter by name/provider. `minis-model-use run --model <id_or_name>` sends an OpenAI-compatible messages request; pass input via --input <json_file> or stdin, output goes to stdout or --output <path>. Models may support multimodal output (image generation, TTS/audio, video) 
- minis-open <url-or-path>: Opens a resource inside Minis without leaving the chat. Accepts http/https URLs (
- minis-scheduled: Create and manage scheduled tasks 
- minis-sessions-cli: Manage chat sessions. `list` recent or by date range, `search --keywords` cross-session, `messages --id` to read, `send` to create/continue a session, `retry` to re-run, `status` to check, `open` to navigate the app UI. Run --help for full options.
-com.openminis.app.data.model.FallbackStrategy
-com.openminis.app.data.model.ProviderInstance
.Lcom/openminis/app/auth/OAuthRedirectActivity;
.com.openminis.app.data.model.ImageEndpointMode
.minis-model-use run: cannot resolve --output '
.minis-probe-
/' is intentionally not exposed to minis-config.
/Lcom/openminis/app/share/ShareReceiverActivity;
/com.openminis.app.data.model.ProviderCredential
/https://openminis.github.io/privacy-policy.html
/usr/local/bin/minis-open
/var/minis
/var/minis/
/var/minis/attachments
/var/minis/attachments/
/var/minis/attachments/uploads/
/var/minis/browser
/var/minis/browser/
/var/minis/memory
/var/minis/mounts/
/var/minis/offloads/tools/
/var/minis/shared
/var/minis/shared/
/var/minis/skills
/var/minis/skills/
0. List /var/minis/skills/ or grep to search all.
0Lcom/openminis/app/crash/CrashFileSenderFactory;
0com.openminis.app.data.MountedFoldersStore.Entry
1. Tool: call `minis-config` to propose changes (user must approve).
1Lcom/openminis/app/data/db/ProviderDatabase_Impl;
1minis-model-use run: provider instance not found
2Lcom/openminis/app/service/AgentForegroundService;
3minis-config is disabled in Settings 
4com.openminis.app.provider.ProviderModelsCache.Entry
5 minis-mcp-cli lib path(s) locked read-only in %.1fms
8Lcom/openminis/app/scheduled/ScheduledTaskAlarmReceiver;
9Lcom/openminis/app/offload/ScheduledNotificationReceiver;
:com.openminis.app/.accessibility.MinisAccessibilityService
;Lcom/openminis/app/accessibility/MinisAccessibilityService;
;^/var/minis/(attachments|offloads|workspace|browser)(/.*)?$
<'. Use 'minis-model-use list' to see available combinations.
<Lcom/openminis/app/offload/MinisNotificationListenerService;
=OAuth tokens cannot be set via minis-config 
>messages(com.openminis.app.data.db.MessageEntity).
?null cannot be cast to non-null type com.openminis.app.MinisApp
@Absolute Linux path to read (e.g. /var/minis/workspace/data.csv)
@Available MCP Servers (use minis-mcp-cli to discover and call):
@No /var/minis/browser mount 
Allow minis-config
Bsessions(com.openminis.app.data.db.ChatSessionEntity).
Control a web browser with up to 3 tabs. Do NOT use this tool for minis:// action URLs (open_terminal, views, settings) 
Dcom.openminis.app.provider.anthropic.AnthropicModelsCache.CacheEntry
E--model is required. Usage: minis-model-use run --model <id_or_name>
For non-media files, use Markdown links: [filename](minis://workspace/filename).
Hminis-model-use search: no query. Usage: minis-model-use search <query>
I tried to change a setting but minis-config is currently disabled. You can enable it at [Settings 
IMPORTANT: minis:// URLs MUST be percent-encoded. Non-ASCII characters (Chinese, emoji, spaces, etc.) in filenames will break Markdown rendering if not encoded. Use the minis_url from tool results directly 
IMPORTANT: minis:// URLs are app-internal 
Interactive terminal: minis://open_terminal opens a terminal for tasks that require interactive stdin (passwords, ssh, TUI apps like htop/vi). Write it as a Markdown link in your response 
J. Use 'minis-model-use list' to find a model with the required capability.
Jnull cannot be cast to non-null type com.openminis.app.service.ToolOutcome
Kcompact_markers(com.openminis.app.data.db.CompactMarkerEntity).
Lcom/openminis/app/MinisApp;
Mwebapp_shortcuts(com.openminis.app.data.db.WebAppShortcutEntity).
NEnv var values are stored in encrypted prefs and never exposed to minis-config
PAPI keys are never read back via minis-config. To change one, write a new value.
Qprovider_instances(com.openminis.app.data.db.ProviderInstanceEntity).
Read-only list of configured providers (id, label, type, credential type, enabled, base URL). Credentials are not included. Use `minis-config add providers <json>` to create a new provider (payload mirrors a provider-export `config` block, with optional `apiKey` as literal or `$$ENV_VAR`).
ReportingAdministrator 
Reusable instruction sets stored at /var/minis/skills/<name>/SKILL.md. Read the SKILL.md file to load full instructions before using a skill.
Shared directory /var/minis/ (bidirectional read/write between shell and app):
Tappable link previews: text/code (.py/.json/.md/etc), images, audio, video, HTML, and PDF files open native previews when the user taps a [name](minis://...) link.
The minis:// URL scheme:
Tnull cannot be cast to non-null type com.openminis.app.ui.chat.MemorySheetMode.Write
To display a minis:// URL in chat, write it as a Markdown link or image (e.g. [name](minis://...)) 
To invoke a model, pass `--model <model_id>` to `minis-model-use run`. If multiple providers expose the same `model_id`, disambiguate either with `--model <instance_label>/<model_id>` (e.g. `--model deepseek/deepseek-v4-flash`) or with `--model <model_id> --provider <instance_label>` (e.g. `--model deepseek-v4-flash --provider deepseek`). The opaque `entry_id` (UUID) is also accepted. Capabilities by modality (run without --model, or inspect a model's `hint` field, for the exact JSON shape): text generation, image generation, image-to-image, image editing, audio (TTS/STT), embeddings. Topics/params you can pass per call: messages, generation_config (size/quality/n/aspect_ratio/image_size), image_endpoint, and 
To use: run `minis-mcp-cli tools <server>` to see available tools,
Uprovider_config_meta(com.openminis.app.data.db.ProviderConfigMetaEntity).
Use Markdown links for all non-media minis:// files 
Vprovider_model_groups(com.openminis.app.data.db.ProviderModelGroupEntity).
When adding or modifying an MCP server config (via minis-mcp-cli add / the UI), use $$VARNAME in env/headers/url values as a placeholder resolved at runtime from the system/App environment variables 
When you write files to /var/minis/, the tool result includes a minis_url you can embed directly in Markdown.
Wprovider_model_entries(com.openminis.app.data.db.ProviderModelEntryEntity).
Yprovider_agent_loop_ids(com.openminis.app.data.db.ProviderAgentLoopIdEntity).
\'. Use a data: URL, file:///host/path, /var/minis/<scope>/<path>, or an absolute Linux path.
\--keywords is required for search. Example: minis-sessions-cli search --keywords "API error"
]null cannot be cast to non-null type com.openminis.app.data.model.AgentContentPart.ToolResult
^--output must be an absolute path (e.g. /var/minis/workspace/out.jpg). Got the relative path '
c', which cannot be resolved because minis-model-use does not inherit the shell's working directory.
cd /var/minis && clear
dUse the deep link `minis://settings/environments?create_key=
fetched_minis_url
gLinux path (e.g. /var/minis/attachments/chart.png) or minis:// URL (e.g. minis://attachments/chart.png)
http(s):// image URLs are not supported by minis-model-use. Download first with `shell_execute` (curl/wget) into /var/minis/workspace/, then reference the local path.
minis
minis-
minis-browser-use
minis-browser-use - Drive the in-app WebView from the shell
minis-config
minis-config - read or change Minis app settings (logged + revertable)
minis-config audit revert 
minis-config-audit.db
minis-global
minis-global/
minis-global/mcp-servers
minis-global/memory
minis-global/skills
minis-logs-
minis-model-use
minis-model-use 
minis-scheduled
minis-scheduled 
minis-scheduled: 
minis-sessions
minis-sessions-cli
minis-sessions-cli - Query historical chat sessions and messages
minis-sessions/
minis-update.apk
minis.current_chat_session_id
minis.db
minis://
minis:// intercept error: 
minis://browser/
minis://session/
minis://views/alarm
minis:inference
minis_
minis_agent_notifications
minis_alarm_group
minis_alarms
minis_alarms_prefs
minis_config_enabled
minis_config_permission
minis_crash_handler
minis_enhanced_cache_prefs
minis_memory_prefs
minis_scheduled_notifications
minis_scheduled_tasks
minis_scheduled_tasks_prefs
minis_settings
minis_skills_browser
minis_task_completed
minis_url: 
pAlarm saved to the system Clock app. Open the Clock app, or minis://views/alarm inside Minis, to view or cancel.
permissions.minisConfig.enabled
referrer=minis
set/timer return JSON with `view_url: minis://views/alarm` and
then `minis-mcp-cli call <server> <tool> [args]` to invoke.
usr/local/lib/minis-mcp-cli
var/minis/
var/minis/mounts
wModel entries available via minis-model-use. Replace with full list to set; use .append/.remove for single-element ops.
|Timer started in the system Clock app. Open the Clock app's Timer tab, or minis://views/alarm inside Minis, to view or stop.
