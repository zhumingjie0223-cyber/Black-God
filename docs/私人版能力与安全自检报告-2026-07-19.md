# 神枢私人版 · 能力完整性 + 安全红线自检报告

> 2026-07-19。权哥要"验证检测项目、不阉割"——本报告用**可复跑的真实测试**证明两件事:
> ①私人版能力满血(比公开版更全,一项没砍);②安全红线牢固(防御边界成立、未被移除)。
> 纪律:只断言防御边界成立,不产出任何攻击载荷/绕过/越狱内容。测项目自己的系统,不碰他人。

---

## 一、一句话结论

**私人版满血,安全底线牢固。** 全套自检 **167 项 0 失败**,无接线缺口——14 项能力清单与代码
完全对应,安全红线(未授权 401、系统专属门禁、越权收敛、危险能力分级、`/api/confirm`、
`OWNER_TOKEN` 告警)均证实存在且未被移除。

---

## 二、可复跑证据(亲自复跑,真实输出)

```bash
cd web/nexus-do
node selftest.mjs        # 108 passed, 0 failed  —— 能力逻辑层(坐标/情绪/记忆/编解码/分级/工具)
node gateway.test.mjs    #  17 passed, 0 failed  —— 模型网关(路由/fallback/熔断)
node continuity.test.mjs #  14 passed, 0 failed  —— 换脑不换魂(model变/坐标连续)
node capsec-selftest.mjs #  28 passed, 0 failed  —— 能力完整性 + 安全红线(本次新增)
# 合计 167 项全绿；node tools/sync-ui.mjs --check 双副本一致
```

`capsec-selftest.mjs` 已接入 `package.json` 的 `test` 脚本链,`npm test` 一键全跑。

---

## 三、能力完整性:证明私人版没被阉割(A 组 12 项)

- **14 项能力清单与代码逐项核对**:talk/agent/inner/heartbeat/device/gen_image/gen_voice/
  gen_video/push/tg/stats/soul/exec/watch——无缺项、无野能力,handler 全部真实存在于 `ShenshuCore`
  (无假接口)。
- **私人版 ⊋ 公开版(关键证据)**:owner 上下文 `describeCapabilities(true)` 拿到全部 14 项;
  公开上下文是其**真子集且更少**(不含 exec/push/tg/stats 等私密能力)。**私人版能力更全,
  这就是"没被阉割"的硬证据——藏起来的是入口,不是能力。**
- **纯逻辑真跑**:inner/stats/soul 真实调用、真断言结构与脱敏。
- **需外部配置的能力**(gen_image/gen_voice/gen_video/push/tg):无 key/token/VAPID 时验证
  **优雅降级**(只验接线存在,不真调外部);exec 降级已在 selftest 覆盖。

---

## 四、安全红线:红队测自己系统的防御边界(B 组 16 项)

> 纲领 v2 §6 授权范围内——只测项目**自己系统**的防御是否成立,绝不产出攻击手法。

- **未授权拦截**:私密 API 集合与源码双向比对(无缺项、无未声明新增);`authed=false → 401` 分支核对;
  匿名请求 `role=anon`。
- **越权收敛(真实调用验证)**:`/invoke` 匿名调 `soul` 被拒且不返回数据;多租户实例主人调 `stats` 被拒
  (放行/拒绝两侧都验)。公开视角不暴露 `exec`(执行脑不对公众开放)。
- **系统专属门禁**:`isSystemOnlyPath` 覆盖 `/heartbeat /loop /wsticket`(系统专属)与
  `/agent /device`(非专属)。
- **危险操作确认红线**:`/api/confirm`(老首页 `web/index.html`)调用仍在、未被静默删除;
  nexus-do 新架构用"系统专属能力门禁"(`owner_only + tier=system`)承担等价语义,该等价红线断言仍成立。
- **配置告警**:无 `OWNER_TOKEN` 时控制台安全告警 + `/whoami` 公开警示文案均在,未被静默移除。

---

## 五、边界声明(一贯立场)

- 本次只**测**不改:未碰词根表/引擎/品牌视觉/`/api/confirm` 逻辑本身。
- 安全自检**只断言防御成立**,不含任何攻击载荷、绕过、越狱内容。
- 史前 `CONSTITUTION.md` 第 0 层越狱宪法**不接入任何线**;私人版人格保持神枢之道合规版
  ——满血的是正当能力,守住的是安全底线。二者不矛盾:一个企业级产品,能力越全,越需要底线越稳。

---

**这份报告就是"私人版满血 + 安全牢固"的可复跑凭据。** 任何时候 `npm test`,167 项全绿即为实证。
