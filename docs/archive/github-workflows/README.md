# 已退役的网页后端流程

2026-09-24 根据当前 README.md 和 STRUCTURE.md 核实：`web/nexus-do/` 已从当前发布架构退出，iOS 直接连接用户配置的模型服务商。

这两个文件完整归档于此，不再由 GitHub Actions 自动执行：

- `deploy-nexus.yml`：此前在枢语变更后试图进入不存在的目录，并触发旧 Cloudflare 部署。
- `nexus-do-ci.yml`：此前检查已不存在的同一后端。

当前原生构建、界面回归和枢语 JS/Python 测试仍保留；枢语同步检查改为验证实际随 iOS 打包的资源。此变更没有删除云端服务、密钥、历史运行或失败通知，也没有声称旧后端已部署成功。恢复旧后端应作为独立任务，先恢复代码并核实部署目标，再启用流程。
