# 任务:全面打磨 → 消费级工业产品落地(权哥 2026-07-12 指令)

背景:权哥已做部分优化,本轮把两仓(Black-God + shuyu-lang)剩余打磨全部收尾。
体检发现:CLAUDE.md 承诺的工程设施(测试、同步校验工具)大半不存在,先补地基。

## 第一阶段:体检(先跑通现状)
- [x] 1. 跑 web/nexus-do 构建 + 自测 —— 83 项自测全过;但后续发现构建产物被 replace 特殊符号毁损(见 8)
- [x] 2. 核对枢语引擎双副本 —— 引擎层(词根表/容量/编解码)完全一致,分叉只在数据层(词库/情绪表,属产品定制)
- [x] 3. 盘点承诺 vs 实际 —— tools/check-sync.mjs、tools/sync-ui.mjs、shuyu-lang 的 npm test/tests/工具全都不存在,本轮全部补齐

## 第二阶段:补工程地基(工业化核心)
- [x] 4. shuyu-lang 补 package.json + npm test —— 12 项 Node 测试(容量/往返/纯净度/词形/追加铁律/造词/词库/跨实现)
- [x] 5. shuyu-lang 补 tools/check-sync.mjs —— 引擎层硬校验(采样 1544 编号覆盖全部 1040 核心行)+ 数据层软警告
- [x] 6. Black-God 补 tools/check-sync.mjs —— 同一工具自动识别两种仓库布局,CLAUDE.md 里的命令现在真能跑
- [x] 7. Python ↔ JS 双实现同构 —— 发现 Python 引擎滞后(20 族/29.5亿 vs JS 52 族/76.7亿),已追加式补齐 32 族并加同构测试

## 第三阶段:产品打磨
- [x] 8. 修复体检发现的问题 —— **线上级 bug**:build.mjs 用 String.replace 注入页面,页面里的 `const $$ = ...`
      被替换规则吞成 `const $`,与已有 `const $` 重复声明,整页脚本语法错误。改函数式替换修复,
      并新增 tools/sync-ui.mjs 双副本校验防回归(正是这个新工具抓到的)
- [x] 9. 文档与实际能力对齐 —— CLAUDE.md 承诺的所有命令(check-sync/sync-ui/npm test)现已全部真实存在且通过

## 第四阶段:收尾
- [x] 10. 两仓提交推送到 claude/project-optimization-polish-35org8,开草稿 PR
- [x] 11. 总结归档 docs/done/,汇报权哥

## 总结

本轮打磨核心成果三件:
1. **修掉一个真·线上级 bug**:构建注入把页面 `$$` 吞成 `$`,导致部署出去的整页脚本因重复声明直接挂掉。
   五个字节的差异,整个 UI 报废——这正是"消费级"和"能跑"的差距。
2. **双实现重新同构**:Python 引擎从 29.5 亿容量(20 核心族)追加式补齐到 76.7 亿(52 族),
   与 JS 侧逐字段一致,并有跨实现测试永久看住。
3. **工程地基从"愿景"变"现实"**:两仓 CLAUDE.md 里写的测试与同步校验命令,之前一半是空头支票,
   现在 npm test(12 项)、python unittest(9 项)、check-sync(跨仓 1544 采样)、sync-ui(防构建失步)
   全部真实可跑、全部绿灯。

遗留决策项(需权哥拍板,工具已如实报警但不擅自动手):
- 词库数据层两仓已各自演化(词族 8↔3、情绪表 43↔12、编号表词条 40↔4 分叉)。
  引擎层不受影响;若要统一内容层,定好"以哪边为准"后跑 `node tools/check-sync.mjs --strict` 驱动收敛。
