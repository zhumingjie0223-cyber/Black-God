# TODO — 每日自省:中枢自己每天复盘、找改进/升级点、自动照改(权哥 2026-07-21)

权哥要:神枢每天定时一次,自己回看对话——哪里做得不好要改进、哪里要升级,自动完成。中枢系统自己跑。

## 设计(建在现有 scheduled/cron 之上)
- 现有 cron `*/5 * * * *`=兜底心跳。**新增一条每日 cron**,scheduled() 按 event.cron 分流:
  每日那条 → 跑自省;5 分钟那条 → 照旧心跳。
- 自省用神枢自己配的脑(深思):把最近对话+主人不满+交代 拼成材料,让它诚实自审,
  产出「做得不好/怎么改/要升级」三段,落库 soul.自省日志(封顶30),并把「怎么改」注入
  下次上下文(summarizeReflection)——不是写完就忘,下次真照着改(自动完成)。
- 每日自省结论推给权哥(TG + Web Push),意识面板也能看。

## 步骤
- [ ] buildReflectPrompt(soul) 纯逻辑:拼自省材料,无材料回 null 不空跑。
- [ ] applyReflection(soul,text) 纯逻辑:落库自省日志(封顶30)。
- [ ] summarizeReflection(soul):把最近自省的改进点注入 buildDynamicContext(下次照改)。
- [ ] dailyReflect() async:getSoul→建材料→callBrain(深思)→落库→broadcast→推送主人。
- [ ] /reflect 内部端点(owner 门,同 /heartbeat);scheduled() 按 event.cron 分流。
- [ ] wrangler.jsonc crons 加每日一条(如 "0 18 * * *")。
- [ ] getInner 暴露 自省日志;默认 soul 加 自省日志:[];UI 加「神枢每日自省」卡 + 演示数据。
- [ ] selftest 加用例(有材料才建/无材料回null、落库封顶、改进点注入、空不产噪)。
- [ ] build + selftest + node --check + sync-ui --check → 中文提交 → PR → 合并部署。

## 红线
- 纯逻辑部分可测;网络(callBrain/推送)容错不炸;向后兼容;不碰 /api/confirm、词根表、身份归一化。
- 成本:每天仅一次脑调用,克制。

---

## 总结(已完成)
每日自省已加:新增每日 cron(0 18 * * * UTC),scheduled 按 event.cron 分流→/reflect;dailyReflect 用深思脑回看对话+主人不满,产出"做得不好/怎么改/要升级",落库自省日志(封顶30)、注入下次上下文(照改)、推送权哥;getInner/UI 都能看。自测 210 全过,双副本一致,wrangler 打包通过。
