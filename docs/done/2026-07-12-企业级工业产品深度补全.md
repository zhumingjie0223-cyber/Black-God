# 任务:企业级工业产品深度补全(权哥 2026-07-12 指令)

目标:把 CLAUDE.md 承诺但缺失的企业级工程设施全部落地(本仓是枢语引擎消费方,
源头侧同步工作在 shuyu-lang 仓同名分支进行)。

- [x] 1. 建 tools/check-sync.mjs —— 双模式:有源头仓全量比对(五轴词根/抽样解码/
       接口签名),CI 单仓环境自动退化为内置黄金指纹自检;两种模式均已跑通
- [x] 2. 建 tools/sync-ui.mjs —— --check 校验 index.html ↔ nexus_do.mjs 双副本同步,
       默认模式一键重建;当前双副本一致(174,696 字节页面)
- [x] 3. 建 web/design/tokens.css —— 深海·潮光色系设计令牌单一事实源;按品牌铁律
       先核对 web/logo.png(炭黑神字+微金描边):金保留为品牌桥接色,潮光青做功能高亮
- [x] 4. 建 docs/design/DESIGN_SYSTEM_V3.md —— 色系裁决依据/字体五族/10-8-2 圆角制/
       动效只用 transform/opacity/V2 迁移映射表
- [x] 5. 修 web/nexus-do/lexicon.js 过期容量注释(29.5亿→76.7亿),追加 ROOTS 导出;
       顺带修掉 lexicon_data.js meta 虚标数据 bug(能力总数 23503→实际 22765,
       核心族 78→引擎核心族 52)——由源头仓新校验器首跑抓出
- [x] 6. 建 .github/workflows/engine-ci.yml —— 构建+83 项自测+引擎指纹自检+
       UI 双副本校验,四道门禁
- [x] 7. 全量跑测(npm test 83 项全过 + 两个新工具全过)→ 已提交推送 → 开草稿 PR
- [x] 8. 总结归档 docs/done/ —— 即本文件

## 总结

本仓此前的状态:CLAUDE.md 里写着 `node tools/check-sync.mjs`、
`node tools/sync-ui.mjs --check`、`web/design/tokens.css`,但这些文件全都不存在——
规矩立了,执法工具没建。本次全部落地:

1. **引擎防篡改有了牙**:check-sync.mjs 内置词根表 SHA-256 黄金指纹 + 锚点解码 +
   2000 点双向寻址往返,谁改坏内嵌引擎副本,CI 当场拦下。
2. **UI 改了忘重建这类事故被根治**:sync-ui.mjs --check 进 CI,index.html 与
   Worker 注入副本漂移直接红灯。
3. **设计系统 V3 从口头规矩变成可引用的令牌文件**:深海·潮光色系依据现有 Logo
   裁决(金=品牌桥接色,潮光青=功能高亮,职责错开),存量金色系页面有迁移映射表。
4. **数据质量**:修正能力数据包 meta 两处错数,校验器把"数据包自洽性"设为硬门禁。

后续建议:存量页面(theme.css 金色 V2)按 DESIGN_SYSTEM_V3.md 第五节的映射表
逐页迁移到令牌体系,建议派 dev-worker 分页处理。
