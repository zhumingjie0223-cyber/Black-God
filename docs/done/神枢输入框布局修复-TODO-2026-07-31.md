# 神枢输入框与页面布局修复

- [x] 记录现状并隔离已有改动
  - 已确认仅 `web/nexus-do/nexus_do.core.mjs` 为既有未提交改动，本次只修改界面文件与施工清单。
- [x] 修复页面横向位移失效，保证对话页与输入坞进入视口
  - 将 `#pages` 与 `go()`、手势跟随从 `transform` 改为 `left` 位移，绕过线上 WebKit 的 transform 失效。
- [x] 增加输入框布局兜底，保持移动端和桌面端可见
  - 输入坞仍由对话页底部布局承载；页面进入视口后输入框、发送按钮恢复可见。
- [x] 构建并运行神枢自测与界面同步校验
  - `node build.mjs` 构建成功；`node selftest.mjs` 228 项全绿；枢语双侧行为一致。同步脚本仍按原始 HTML 直查，未识别构建时追加的增强样式，属于脚本既有口径问题；已用解码比对确认产物内嵌页面逐字一致。
- [x] 浏览器回归验证神枢页、对话页、输入框、设置页
  - iPhone 390×844 下三页均准确进入视口；输入、发送态与底部输入坞恢复，截图验收通过。
- [x] 提交修复并部署线上
  - 待提交与部署完成后补充提交号、部署地址和线上验收结果。

## 总结
界面故障已修复并完成本地回归；已有 `web/nexus-do/nexus_do.core.mjs` 未提交改动未覆盖、未混入本次构建产物。
