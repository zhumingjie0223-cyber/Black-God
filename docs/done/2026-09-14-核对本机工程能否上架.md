# 核对本机工程能否上架 App Store

- [x] 摸清当前工程名、版本号、Bundle ID、签名、图标、上架材料
  - 当前检出分支是 `cursor/dev-env-setup-899b`：工程名 `BlackGod888`，版本 `1.1.0` 构建 `2`。
  - 真正的产品在 `main`：工程名 `BlackGod`，版本 `1.2.0` 构建 `8`，Bundle ID `com.blackgod.nexus`，App ID `6809828368`。
- [x] 核对本机能否生成工程 / 是否具备运行时产物
  - 本机有 Xcode 26.6、xcodegen、fastlane；有 `.runtime-build` 与 `Runtime/AlpineRootfs`、`Runtime/Libs`。
  - 当前旧分支 `project.yml` **不包含** Runtime/ShuyuRuntime；`ShuyuRuntime` 工作区缺失。这条旧分支编出来的不是现在的正式 App。
- [x] 对照 App Store 硬要求列能上架 / 还缺什么
  - 图标 1024×1024 无 alpha、5 张 6.7″ 截图 1290×2796、隐私政策网址 HTTP 200、商店文案都在。
  - `main` 上构建 `3` 曾上传并处理完成；**从未提交 App Review**（Connect 网页当时 502 / 鉴权失败）。
  - 还缺：切回 `main`、上传构建 8 或选用已处理的构建 3、审核演示 Key、Connect 里填分级/隐私标签/价格并点提交。
- [x] 给权哥一句结论
  - **现在还不能算已上架，也不能直接点提交就当完事。** 材料到了门口，审核没交；而且当前检出的是旧分支，不是正式版。

## 总结

本轮只做核对、不改代码。结论：仓库里的正式产品是 `main` 的 Black God 1.2.0（构建 8），不是当前检出的 1.1.0/BlackGod888 旧线。代码和商店材料已经齐到能交审核的门口（图标、文案、截图、隐私链接可用，构建 3 还上传过），但 App Store 审核没有提交，所以现在 **还没有上架**。
