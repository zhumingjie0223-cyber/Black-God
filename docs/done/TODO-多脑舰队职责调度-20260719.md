# TODO — 多脑阶段二:职责分派 + UI(权哥 2026-07-19「做呀·神枢系统主导」)

原则:**神枢系统始终主导**——底层脑是它调遣的算力,神枢决定派谁、归一身份、拥最终输出。
目标:不乱(职责写死·派发确定)+ 秒回(轻任务直接走快答脑,不唤醒慢脑)+ 分工(每条脑设职责)。

## 职责方案(权哥认可)
快答 / 代码 / 深思 / 主力 / (兜底=任意在线+CF)
- 闲聊简单→快答;写代码→代码;难题深推理→深思;日常默认→主力;都挂→兜底 CF,永不失语。

## 步骤
- [ ] 1. 后端:brain 加 role 字段(resolveBrains/setConfig/getConfig)
- [ ] 2. 后端:orderBrainsForTask(brains, role)——对口职责优先,其余作故障转移(神枢主导:总能兜底)
- [ ] 3. handleTalk 按 tier+caps 算 preferredRole,经 opts.role 传入 callBrain→tryGateway 用有序脑
- [ ] 4. UI 阶段二:设置里增删改 2~9 条脑,每条 地址+key+模型+职责下拉+开关,带一键粘贴
- [ ] 5. build+selftest(加职责路由测试)+双副本+真机(Moonshot 至少一条)→ 提交 → PR → 合并
- [ ] 6. 归档

## 红线
- 神枢主导:身份归一化/诚实报错不动;向后兼容旧单网关;/api/confirm 不动

## 完成(2026-07-19)
阶段二 UI + 后端职责路由全部完成:
- 后端:brain 加 role;orderBrainsForTask(对口职责排前+故障转移);preferredRole(tier/caps→职责);
  handleTalk 传 opts.role;setConfig/getConfig 带 role。神枢主导:总能兜底,永不卡死。
- UI:设置「算力接口」入口改开「大脑舰队·神枢调度」sheet——增删改 1~9 条,每条 标签+职责下拉+
  启用开关+一键粘贴+地址+key+模型;掩码沿用原 key。无头浏览器渲染验证,零报错。
- selftest 147 过(+8 职责路由测试)·双副本一致·语法 OK。向后兼容旧单网关。
