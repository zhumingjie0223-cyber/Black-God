# 黑绿界面重构与 Shell 直播验收（2026-09-24）

本轮用户要求：检查已上架 Black God iPhone／iPad 应用并落实可靠性改进；直接重构按钮、界面与面板，原黑绿主色不变；移除独立工具页；减少会话说明文字，增强悬浮感与动态光效；Shell 执行时自动出现能看到真实输出的小直播框。

工作分支：`codex/industrial-upgrade-20260924`，基线 `f184208`。独立目录 `/Users/aquan/src/Black-God-industrial-20260924`。原目录 `/Users/aquan/src/Black-God` 保持干净。商店公开版本1.2.1，App ID 6809828368，Apple公开发布时间2026-09-21T20:30:16Z；本轮没有上传、提交审核或改变商店版本。

## 已完成清单

- [x] 核实工程、公开发布版本及本地构建状态，建立独立工作目录。
- [x] 检查并修复会话数据、任务恢复、工具输入、发送许可、审计与持续集成中的具体问题。
- [x] 从源码重新构建内置运行库与签名验证、版本锁定的 Alpine 镜像，修复 host 首次编译的头文件依赖顺序。
- [x] 统一按钮、字号和悬浮组件，保留原 bgJade、bgJadeHi、bgDark 数值。
- [x] 主导航改为对话／创作／监测／我的；工具能力进入我的→高级设置；增加原生 iPad 侧栏和横竖屏配置。
- [x] 会话空态减字、加入动态轨道光球与悬浮输入；正文保留，计划、执行详情和依据按需展开，错误与恢复动作直接可见。
- [x] 接通真实 Shell 小直播、实时标准输出/错误输出、停止、详情和完成后收起；洪峰输出不会挤掉最新命令。
- [x] 完成相关单元、界面、真实内置 Shell、资源一致性及 Release 构建检查，修复失败并定向复验。
- [x] 人工检查手机、iPad横竖屏、Shell运行中及完成后的原生截图，保存手机动态录像。
- [x] 同步当前架构、公开版本区别、验证结果与后续发布缺口并归档。

## 实际修改

会话清空先记录意图，重启继续完成，迟到回复不能复活已清除消息。损坏聊天/检查点阻止覆盖，删除前保留恢复副本；备份失败则阻止清除。重新生成成功保存前保留旧答复，检查点记录替换编号，恢复时只替换一次。

工具每轮最多8次，正文和工具参数按UTF-8累计2MB限制，流式和非流式适用；原生回复保留协议身份。纯工具JSON兼容不会将解释性示例误当执行指令。发送许可在OAuth刷新后再次检查。审计最近2000条轮转并累计移出数量，未完成调用另行记录。

UI主页面和模型连接面板重新组织，长帮助默认折叠。创作页明确生成服务未接入，监测页保留真实分母，无样本不显示虚假百分比。手机四页悬浮导航，iPad使用侧栏；主配色值完全保留。光效随“减少动态效果”静止，非活跃状态暂停。

普通执行只占一条状态，收到工具接受的Shell命令后显示约160pt的小直播。准备、空间检查、运行状态来自实际回调；命令及输出有显示预算，最新命令单独保留已脱敏且不超过1200字符的副本。任务结束后收起，通过顶部菜单打开当前保留的详细记录；聊天和演练记录分开选择。

验收中还修复了准备阶段取消缺陷：已有大量文件时扫描发生在PID生成前，旧实现进入后台不能阻止之后启动。现在扫描异步且可取消，取消传至准备任务，启动前再次检查；旧扫描观察任务不能取消新的命令。

## 验证证据

所有原始日志、结果包、图片与录像位于：
`/Users/aquan/Backups/blackgod-industrial-20260924/validation`

| 范围 | 实际结果与记录 |
|---|---|
| iOS单元整轮 | `final-runtime-unit.xcresult` / `.log`：322项通过，含准备阶段取消、存储扫描与恢复回归 |
| 直播增量 | `final-targeted.xcresult`：8项中7项通过，包含新增的命令滚动保留与原生会话Shell截图；旧首输出测试5秒等待未覆盖冷扫描，延长等待但保留“进程未结束即有输出”断言，`delivery-rechecks.xcresult`对应单元通过 |
| 不同单元数量 | 共324个不同单元用例获得通过结果（322整轮+2新增）；不是最终源码324项全量重跑 |
| iPhone界面 | `final-ui-v2.xcresult`：17项中15通过，2失败（3条断言）；后台/强退恢复在`final-targeted.xcresult`通过；枢语/运行空间/演练在`storage-ui-final.xcresult`通过，17个不同场景均有通过记录 |
| iPad界面 | `aura-tablet.xcresult`：2项通过，检查四页导航、侧栏、横竖屏、输入和连接面板；其中主页面场景与手机重复，不累计成新的独立用例 |
| 枢语双实现 | `shuyu.log`：123项JS、55项Python通过；最终资源一致性检查通过 |
| Release | `release-delivery.log`：最终源码设备目标无签名构建成功，非签名归档/上架证明 |
| 运行环境 | `runtime-build.log`、`runtime-image.log`、`runtime-final.log`；镜像`alpine-3.22.5-security1-arm64`，SHA-256 `7cc4ba21c4972f782dfa2a53b8dde2f63ead832670ef22fc4edf3e21845059fb` |
| 静态检查 | 最终`git diff --check`通过；原始工作目录无改动 |

UI失败和处理：强杀恢复测试原本在准备阶段强杀，进程尚未登记；改为等待实际执行状态，并只断言最新记录。叠层面板新增后，滚动与“完成”查询改为对应页面；只读容量文字按实际可见区域判断，不能要求其像按钮一样接受点击。模拟器横屏使用应用截图时曾生成裁剪异常，改用全屏截图并人工检查，最终横屏图完整。失败记录保留，没有删去失败来伪造首次全绿。

## 预览真实性

`交付预览/`内图片来自原生界面，未裁剪、绘制或合成。手机/iPad页面来自UI自动化或模拟器；Shell截图来自挂载的原生ChatView验收窗口，真实命令通过生产工具与运行时执行，确认首输出出现时进程尚在运行。

示例用户问题为商品求和，结果由真实Shell的TOTAL=39得到；完成画面的回答由测试按实际输出构造，没有调用真实服务商模型。因此这些图证明界面和工具流，不作为模型能力或真实模型闭环的证据。录像`黑绿动态界面-iPhone.mp4`来自模拟器录屏。

## 留待发布前完成

物理iPhone/iPad、多任务尺寸、真机功耗和长跑；真实拟发布模型/授权刷新验收；签名、构建号、公开隐私政策同步和商店提交。新增原生iPad界面目前只在本地分支，商店1.2.1不因本轮修改自动更新。

共享App Intents任务服务、硬磁盘配额、多会话隔离、完整数据导出、浏览器视觉操作等仍列在验收门槛中。本轮没有修改模型权重，也没有把光效或固定样例通过描述为智能提升。

总结：已交付本地黑绿UI重构、Shell真实小直播及确认问题的可靠性修复，完成与本轮改动相关的构建和模拟器验证；不宣称达到未验证的工业认证或真机长期稳定性等级。
