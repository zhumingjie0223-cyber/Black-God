- [x] 读取并确认 AGENTS/CLAUDE/README 与相关代码测试入口，锁定最小改动点（结果：已确认主改动位于 web/nexus-do/nexus_do.core.mjs、nexus_provider_adapter.mjs 与 gateway/provider 相关测试。）
- [x] 梳理普通对话与 Agent 网关调用链路，识别可共享的请求/错误处理实现（结果：已定位 callBrain、callGateway、probeModels/probeModelsPublic 存在重复且不一致的网关请求与错误处理。）
- [x] 实施最小代码改动：统一网关调用可靠性（URL 规范化、超时/取消/重试边界、SSE/JSON 错误处理、诊断信息）（结果：已新增统一网关 URL 校验与 JSON 请求封装，接入超时/取消透传、有界重试与无效 JSON 诊断；修复 SSE 分块/断流处理。）
- [x] 增加并完善 mock 上游回归测试（成功、分片、取消、超时、401/429/5xx、无效 JSON、断流）（结果：已扩展 gateway.test.mjs 与 nexus_provider_adapter.test.mjs 覆盖对应场景并通过定向测试。）
- [x] 执行要求的构建/自测/测试并记录无法执行项（结果：已执行 node build.mjs、node selftest.mjs、npm test，全部通过；无未执行项。）
- [x] 更新必要文档与变更摘要，确认无敏感信息泄露（结果：已更新 web/nexus-do/DEPLOY.md 网关配置约束，并完成改动文件 secret scanning。）
- [x] 收尾：同步构建产物、运行并通过最终校验与安全检查（结果：已同步 nexus_do.mjs 构建产物，parallel_validation 与本地测试通过。）

总结：本次在不改产品定位/人格/UI 的前提下，聚焦网关调用可靠性做了最小闭环改造：统一 URL 校验与请求错误处理、补齐超时取消与有界重试策略、强化无效 JSON 与 SSE 分块/断流诊断，并用 mock 回归测试覆盖关键成功与失败路径。
