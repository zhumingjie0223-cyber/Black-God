# Black God 智能升级

用户最新要求：统一名称 Black God，去掉 888；主要升级智能能力。

- [x] 统一应用、工程和活跃文档名称，保留 Bundle ID 与用户数据兼容。
- [x] 接通按目标生成计划、工具执行、结果汇总与有界复核修正。
- [x] 改善中文记忆检索和上下文预算，区分用户记录与模型推测。
- [x] 阻止重复无效工具循环，清晰报告预算耗尽与未验证结果。
- [x] 离线场景验证通过：11 项智能专项 + 9 项基础回归，共 20 项 iOS 测试通过。
- [x] BlackGod 工程无签名 Release 构建通过，最终构建日志无警告/错误，git diff --check 通过。

## 总结

产品与工程统一为 Black God；Bundle ID 保持 com.blackgod.nexus，保证既有数据兼容。接通按目标规划、工具执行、跨步上下文、结果汇总、模型复核与一次修正；简单问答只需一次请求。支持时间、计算、用户记录检索，重复参数不会反复执行，单任务最多 20 次模型请求。快捷指令使用相同执行链路并返回实际答复。

中文记忆使用重叠词面检索，默认召回用户原话，不将模型生成内容当作用户事实。历史上下文有字符预算，工具结果进入复核证据。复核是同模型的独立一次请求，不等于事实已由外部来源验证。

验证：20 项 iOS 测试通过；此前未改动的枢语 84 项 JS、33 项 Python 测试通过。未用真实密钥联调，未做事实准确率或真实任务成功率对比，也未发布或提交审核。本轮不包含联网搜索、设备控制或新模型训练。
