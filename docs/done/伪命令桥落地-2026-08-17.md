# TODO — 伪命令桥（容器执行脑 ⇄ 神枢能力，权哥已拍板）（2026-08-17）

- [x] 读透现有链路 —— task_runner 是容器内 HTTP 服务，DO 经 _containerFetch 单向调用；顺带实锤两处字段错位真 bug（DO 发 cmd/读 code，runner 收 command/回 exitCode——exec 链路对不上）
- [x] 设计 —— 沙箱伪命令 → 本机 /bridge（runner 代持令牌）→ 神枢 /bridge 路由 → 能力白名单分发；桥令牌=OWNER_TOKEN 单向哈希，机密不进沙箱；未配 OWNER_TOKEN 桥关死
- [x] 容器侧 —— task_runner 加 /bridge 端点与信封接收；bin/ 下 nexus.mjs + nexus-memory / nexus-shuyu / nexus-soul 三条伪命令进镜像；顺修 command/cmd、exitCode/code 双向兼容
- [x] DO 侧 —— /bridge 路由（X-Bridge-Token 校验 fail-closed）+ handleBridge 白名单（memory.search / memory.note / shuyu.query / soul.status，执行/导入/设备控制一概不上桥）+ _withBridgeEnvelope 出门信封
- [x] 测试 —— 新增 bridge.test.mjs 13 项全过；全量回归 86 项单测 + 236 项自测 + 双副本同步全绿
- [x] 提交推送 + 新 PR（#95 已合并，分支已重置到最新 main）
- [x] 汇报 + 归档

---

## 总结

伪命令桥装好了：沙箱里敲 `nexus-memory search 咖啡` / `nexus-memory note 内容` /
`nexus-shuyu 词` / `nexus-soul`，JSON 信封一路回连神枢，Minis 的原生卸载模式神枢版。
安全线：白名单只开只读+追加能力，桥令牌是主人令牌的单向哈希，沙箱永远拿不到本体。
顺手修掉容器 exec 链路两处字段错位真 bug（cmd/command、code/exitCode）。
注意：容器侧改动要随镜像部署——当前 CI 的 Cloudflare 令牌推不动容器镜像（连续 Unauthorized），
权哥补令牌权限后镜像才会更新；DO 侧路由随脚本部署即生效。
