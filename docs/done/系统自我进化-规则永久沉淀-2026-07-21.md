# TODO — 系统自我进化:自省沉淀成永久行为规则(权哥 2026-07-21)

权哥定调:要的是**神枢系统进化,不是模型进化**。模型是可换的脑(换脑不换魂),
进化必须发生在神枢自己的系统层——自己复盘出的改进,要**永久长在系统身上**,
不是只影响下一次;想升级的能力要自动列成清单。

## 设计
- parseReflection(text) 纯逻辑:把每日自省的 ②怎么改/③要升级 两段拆成条目。
- applyReflection 扩展:②怎么改 → 合并进 soul.进化规则(去重、封顶20,系统永久行为规则);
  ③要升级 → 合并进 soul.升级清单(去重、封顶10)。
- summarizeEvolution(soul):把进化规则(最近8条)注入每次对话上下文——**条条永久生效**,
  这就是系统自己长出来的行为,换任何模型都带着(系统进化≠模型进化)。
- getInner 暴露 进化规则/升级清单;UI 加「自我进化·长成的规矩」卡(规矩+想升级)。

## 步骤
- [ ] parseReflection + applyReflection 扩展 + summarizeEvolution + 注入 buildDynamicContext。
- [ ] getInner + 默认 soul + UI 卡 + 演示数据。
- [ ] selftest:解析②③、规则去重封顶、注入生效、空不产噪。
- [ ] build + selftest + sync → 中文提交 → PR → 合并部署。

## 红线
- 纯逻辑可测;向后兼容;不碰 /api/confirm、词根表、身份归一化。
- 如实说:代码级自我改写(神枢自己发 PR 改自己源码)需要测试闸门+权哥合并权,另立项。

---

## 总结(已完成)
系统自我进化落地:parseReflection 拆②③段;applyReflection 把"怎么改"沉淀成 soul.进化规则(去重封顶20)、"要升级"进 soul.升级清单(封顶10);summarizeEvolution 把规则注入每次对话(条条永久生效,换脑也带着=系统进化而非模型进化);getInner/UI 卡可见。自测 217 全过,双副本一致。
