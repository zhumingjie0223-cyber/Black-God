# 排查 gpt-6-astra 又报协议错

- [x] 1.1 核对协议桥/隧道进程与监听端口是否仍在跑 — 桥 8082 健康；cloudflared 在跑；隧道 `corrections-clay-rebates-floral.trycloudflare.com` 健康
- [x] 1.2 核对 Cursor 自定义模型是否仍指向隧道地址 — 已漂回直连 `https://apiclaude.cc/v1`，这就是报错根因
- [x] 1.3 复现：本地经桥打一发 Agent 形状请求 — 隧道 `/v1/chat/completions` + `gpt-6-astra` 返回 200（pong）
- [x] 1.4 给出权哥可立刻操作的修复结论 — 已强制写回隧道基址；需 Cmd+Q 完全退出再开 Cursor 让内存态生效

## 总结

报错 `gpt-6-astra` / `chat_completions` 不可路由，不是模型挂了，是 Cursor 基址又指回了直连网关。协议桥与隧道都正常；已把 `openAIBaseUrl` 写回隧道，并挂了退出后重写守护。权哥请完全退出 Cursor 再开，新对话选 `gpt-6-astra`。
