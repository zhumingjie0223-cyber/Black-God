# TODO — 自演化再校准:弱技能自动删除(权哥澄清 2026-07-21)

权哥澄清第二条的真意:**不是只"保护强的不被覆盖",而是弱技能要被替换/自动删除**——
技能库主动淘汰弱的,只留强的,越淘越精("越来越强不是越来越平庸")。

## 规则(在已有基础上加)
- 已有:①用满 3 次才写入(候选区);②验证方法不被未验证覆盖、强度只增(保留)。
- **新增:弱技能自动删除**——每次写入/强化时扫一遍正式库,把「弱且没用起来」的清掉:
  - 判定"弱" = 未验证 且 强度低(≤ 毕业基线)且 久未命中(过期,默认 30 天没再用到)。
  - **验证过的技能永不自动删**(实证做成=强,受保护)。
  - 常用的(强度随 count 涨)也不删。只删"勉强毕业又再没用过、还没验证"的僵尸技能。

## 步骤
- [ ] skillUpsert 加弱技能自动删除扫描(未验证 + 强度≤3 + 过期)。
- [ ] selftest 加用例:弱且过期的未验证技能被自动删;验证技能不被删;常用/新近的不被删。
- [ ] build + selftest + node --check + sync-ui --check → 中文提交 → PR → 合并部署。

## 红线
- 验证技能受保护绝不误删;纯逻辑;向后兼容;不碰 /api/confirm、词根表、身份归一化。
- 阈值(过期天数/强度线)先给保守默认,权哥要更狠再调。

---

## 总结(已完成)
弱技能自动删除已加:每次写入扫描,未验证+强度≤3+过期(30天没再用)的僵尸技能自动清除;验证过的、常用的、新近的受保护绝不误删。自测 203 全过,双副本一致。
