# 全面体检任务清单（权哥指令：每个符号 + 所有功能全测）

- [x] 1. 词根表全量核查——五轴 1372 个词根逐个查重复/空值/非法字符/汉译纯度，零问题
- [x] 2. 每个符号往返测试——全部词根 + 组合覆盖 + 边界 + 10 万大样本共 101,386 次往返，全部成立
- [x] 3. Python ↔ JS 跨引擎一致性——6,384 条逐词比对（覆盖五轴全部索引+边界），完全一致
- [x] 4. 消费副本 web/nexus-do 引擎同一套全量扫描，同样零问题
- [x] 5. 全部 Node 测试文件跑完（apple-tool/continuity/gateway/active_inference/cognitive_v2/gw_workspace/self_model），13 项全过
- [x] 6. Python 引擎测试 9 项全过
- [x] 7. selftest.mjs 228 项全过 + capsec-selftest.mjs 安全自测 38 项全过 + 枢语套件 27 项全过
- [x] 8. check-sync 引擎层完全同步；sync-ui 曾报"主界面失步"——查明是校验工具自身 bug（见 9）
- [x] 9. 发现并修复 1 个真 bug：tools/sync-ui.mjs 拿原始 index.html 与构建产物比对，但构建时会先注入 nexus-frontier.css，导致该检查自注入功能上线起永远误报失步、形同虚设。已让校验复刻构建变换，修后双副本校验真实通过
- [x] 10. 提交推送，更新 PR #88

## 总结

权哥要求"每一个符号 + 所有功能全测"。结果：

**符号层（零问题）**：五轴 1372 个词根逐个核查（重复/空值/非法字符/汉译纯度）全干净；
每个词根 + 组合覆盖 + 边界 + 10 万大样本共 101,386 次 encode↔decode 往返全部成立；
Python↔JS 跨引擎 6,384 条逐词比对完全一致；消费副本引擎同样全量扫描零问题。

**功能层（315 项测试全过，修复 1 个工具 bug）**：selftest 228 + 安全自测 38 + Node 套件 13
+ 枢语套件 27 + Python 9 = 315 项全部通过。唯一真问题是 sync-ui.mjs 校验规则与
build.mjs 注入规则不一致造成的永久误报，已修复并复测通过。

结论：引擎与功能本体是健康的。若线上（Cloudflare）表现有问题，大概率出在部署链路
（根目录设置 web/nexus-do、OWNER_TOKEN 密钥两个待办），不在代码本体。
