# TODO — 自演化加深:技能"三次才写入·写入只增强"(权哥 2026-07-21)

权哥定的两条铁规:
1. **一门技能必须用满 3 次以上才准写入正式技能库**(3 次以下只是"苗子",不占正式位、不影响回答)。
2. **写入后只能越来越强,绝不被平庸覆盖**(验证过的方法不许被未验证的顶掉;强度只增不减)。

## 步骤
- [ ] skillUpsert 引入「候选区 候选{}」:新技能先进候选累计次数,`count>=3` 才毕业进 `技能{}`。
- [ ] 毕业/强化时防平庸:验证过的方法(实证做成)是硬通货,未验证的不许覆盖;同档留更完整的;`验证` 一旦为真永远真;新增 `强度` 字段只增。
- [ ] 淘汰律(超上限才淘汰)按 `强度` 排,验证过的更难被淘汰(不是简单按 count)。
- [ ] getInner 暴露 `技能苗子`(候选数),让"还在攒的"看得见;`已习得技能/总数` 仍只算毕业的。
- [ ] 默认 soul 初始化加 `候选:{}`;向后兼容(旧数据无候选安全降级)。
- [ ] selftest 加用例:用 2 次不写入/第 3 次才写入、验证方法不被未验证覆盖、强度只增、候选不进召回。
- [ ] build + selftest + node --check + sync-ui --check 双副本一致 → 中文提交 → PR → 合并部署。

## 红线
- 纯逻辑;向后兼容;不碰 /api/confirm、词根表、身份归一化。显式事实记忆(记住/叫我)不受影响(那是另一条线,即时生效)。

---

## 总结(已完成)
技能三次才写入 + 写入只增强:skillUpsert 引入候选区(count>=3 毕业)、验证方法防覆盖、强度只增、按强度淘汰;getInner 暴露技能苗子;UI 显示苗子数。自测 199 全过,双副本一致。
