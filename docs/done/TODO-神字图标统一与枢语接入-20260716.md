# TODO · 神字图标统一 + 游客链路接入枢语

来源：权哥截图反馈（2026-07-16）
1. 桌面/App 图标是"绿底白宋体神"，不是品牌神字（玄墨浮雕+人影）——"这个神不太对"。
2. 登录页黑圆球虽用品牌神字，但 `center/cover` 塞圆形，笔画被裁。
3. 游客对话（/pubtalk）提示词一个字没提枢语，模型被问"枢语是什么"只能瞎编——"没接到枢语"。

## 清单

- [x] 从品牌资产生成新图标——用 `web/logo.png` 原图抹掉底部标语后裁出：512/192 方形（神字占边长 74%）+ 384 圆形安全版（外接圆占直径 84%，圆裁零切笔画）
- [x] 替换 `web/nexus-do/icon_asset.mjs` 的 ICON_PNG_512_B64 / ICON_PNG_B64——绿底白神 → 品牌玄墨神
- [x] 替换 `web/nexus-do/nexus_do.core.mjs` 的 ICON_SVG——改为内嵌 192 品牌位图（复用资产，零体积重复），注释与实现终于一致
- [x] 替换 `web/nexus-do/index.html` 内嵌 `--logo`——换圆形安全版，登录圆球/顶栏不再裁笔画
- [x] `PUBLIC_SYSTEM_PREFIX` 加入枢语自述（五维乘法语义空间 · 76.7 亿可寻址词，实测 CAPACITY=7,667,712,000）+ /pubtalk 每句临场推枢语坐标注入提示词
- [x] 重新构建 `node build.mjs`（408708 字节）· `selftest.mjs` 108 过 0 挂 · `sync-ui.mjs --check` 双副本一致 · gateway 测试过 · `check-sync.mjs` 引擎层完全同步
- [x] 中文 commit，推送 `claude/character-issue-s1mbd4`，开草稿 PR

## 总结

两个问题一个根：Worker 里自己长出了"第二个神"。图标层面，`ICON_SVG`/`icon_asset.mjs`
画的是绿底白宋体神字，跟品牌资产（玄墨浮雕神字+人影）完全两回事，注释还写着"玄墨底"；
登录圆球虽用了品牌图，却被 `center/cover` 圆裁切掉笔画。本次全部统一回品牌神字：
桌面图标、/icon.svg、/apple-touch-icon、登录圆球、顶栏徽标看到的是同一个神，
圆形位置用专门的安全边距版本，笔画完整。枢语层面，游客对话走的 `PUBLIC_SYSTEM_PREFIX`
对枢语只字未提，模型只能现编格言；现在提示词写明枢语是五维乘法语义空间（76.7 亿可寻址词），
并且每句话都临场推演一组五维坐标注进去——公共版神枢也说得出自己的语言了。
注意：线上要重新部署（wrangler deploy）才生效；iOS 桌面图标已添加的用户需删掉重新添加。
