# TODO · iOS App Store 上架材料（中英双语）

- [x] 1. 创建 `ios-app/AppStore/metadata/zh-Hans/`：name / subtitle / description / keywords / promotional_text（已完成，字符数均在 Apple 限制内：名称 12/30、副标题 22/30、关键词 62/100、推广 77/170、描述 791/4000）
- [x] 2. 创建 `ios-app/AppStore/metadata/en-US/` 英文版同上五个文件（已完成，名称 15/30、副标题 29/30、关键词 95/100、推广 154/170、描述 1761/4000）
- [x] 3. 创建 `ios-app/AppStore/metadata/privacy_url.txt` 与 `support_url.txt`（已完成，另附 PRIVACY_POLICY.md 中英双语供隐私链接指向；支持链接指向 GitHub Issues）
- [x] 4. 编写 `ios-app/AppStore/SUBMIT_GUIDE.md` 上架步骤指南（已完成，含准备清单、打包上传、App Store Connect 填写、隐私标签、出口合规、审核备注模板、常见拒审应对、后续版本更新）
- [x] 5. 新增 `zh-Hans.lproj/Localizable.strings` 与 `en.lproj/Localizable.strings`，`BlackGod888App.swift` 接入多语言（已完成：新增 `L10n` 工具、`AppTab.titleKey`，RootView Tab 标题走本地化，NexusError 错误文案走本地化；另加 InfoPlist.strings 让桌面名「神枢/Nexus」与权限说明双语）
- [x] 6. `project.yml` Bundle ID 改为 `com.blackgod.nexus`，版本 1.0.0，清理已删除的 VPN 扩展 target，同步 pbxproj（已完成：bundleIdPrefix 改 com.blackgod；去掉引用不存在目录的 NexusPacketTunnel target；去掉 NSAllowsArbitraryLoads 与 Bonjour 声明；加 ITSAppUsesNonExemptEncryption=false 与 CFBundleLocalizations；entitlements 清空无用 App Group。旧 pbxproj 引用的源文件全部不存在、无法构建，按"先归档再删"原则移到 `docs/archive/ios-app-stale-xcodeproj-2026-09-05/`，生成物加入 .gitignore）
- [x] 7. 语法/一致性自检（已完成：strings 文件逐行语法校验通过，中英 key 集合完全一致、占位符数量一致、代码引用的 key 均有定义；project.yml YAML 解析通过；entitlements plist 解析通过；Swift 括号配平通过。本机无 Swift 工具链，未做编译，需在 Xcode/CI 里跑一次 `xcodegen generate && xcodebuild`）
- [x] 8. 中文 commit，push 到 main，TODO.md 归档到 `docs/done/`

## 总结

本次为 iOS App 首次上架准备了完整材料：`ios-app/AppStore/` 下有中英双语商店文案（五项全部在 Apple 字数限制内）、隐私政策、支持链接和一份可照做的上架指南（含审核备注模板，重点解决"审核员没 Key 判定功能不可用"的最常见拒审场景）。代码侧新增中英 `Localizable.strings` + `InfoPlist.strings`，`BlackGod888App.swift` 提供 `L10n` 入口与 Tab 本地化 key，错误文案随系统语言切换。工程真源 `project.yml` 固定为 Bundle ID `com.blackgod.nexus`、版本 1.0.0，删掉了已不存在的 VPN 扩展 target 和会引起审核质疑的任意明文加载开关，失效的旧 xcodeproj 归档而非直接删除。

待权哥处理：
1. 仓库需保持 Public，否则隐私政策链接审核员打不开；若要私有，把 `PRIVACY_POLICY.md` 发布到任意公开网页后改 `privacy_url.txt`。
2. 首次打包前在 Xcode 里建 `Assets.xcassets` 放 1024 图标（指南第 1 节已写）。
3. 其余 View（ToolsView/MediaView/MonitorView）文案仍是中文字面量，本次只收录了核心路径词条，后续可按 `Localizable.strings` 顶部注释的规则逐步迁移。
