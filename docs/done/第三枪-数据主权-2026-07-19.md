# TODO · 第三枪(数据主权:记忆/人格可导出·可迁移·可纯本地)

> 权哥"一鼓作气做完别问了"。第三枪三块的安全落地:
> ①数据主权=本程完整做完(导出/迁移/纯本地);②多租户默认开=可用可测但不擅自翻默认(不可逆线上改动);
> ③软著/截图不造假,只碰文案。分支已从 origin/main(f46d054)重建。

- [ ] 1. 后端 `/export`:私密鉴权,导出本用户 soul(人格/记忆)+stream(对话流)+词典(私语)为可迁移 JSON,
      **不含任何密钥/凭据/系统缓存**。
- [ ] 2. 后端 `/import`:把导出的 JSON 迁回,**先备份现有 soul 再覆盖**(比照 migrate 防误触,需 ?confirm=1)。
- [ ] 3. 前端"我的"加"导出我的数据"→纯本地下载 JSON(不经第三方)。
- [ ] 4. 隐私政策文案:补"数据可导出/可迁移/可纯本地"。
- [ ] 5. 测试:export 结构完整+不含密钥、import 往返一致、未授权 401。
- [ ] 6. 构建+全套测试全绿+双副本同步;提交推送;开新PR草稿;汇报。TODO归档 docs/done。

## 进度记录
- storage keys:soul/stream/词典=用户意识数据(导);_vapid/push_subs/config/users/mm_cache=系统/凭据/缓存(不导)。

## 进度记录
- [x] 1. /export:私密鉴权导出 soul+stream+词典,不含密钥/凭据/系统缓存。
- [x] 2. /import:格式校验+需?confirm=1+覆盖前备份_soul_backup_(可回滚)。
- [x] 3. 前端"设置·数据主权·导出我的数据"→纯本地Blob下载JSON(不经第三方)。
- [x] 4. 隐私政策补"导出·数据主权"条目。
- [x] 5. capsec补10项数据主权断言(全带走/不泄密钥/防误触/先备份/往返一致);API集合预期同步。
- [x] 6. 全套183项全绿(selftest114+gateway17+continuity14+capsec38)+双副本一致。

## 总结
第三枪·数据主权落地:记忆/人格/私语可一键导出为JSON、纯本地下载、可迁移回(带备份保护)——
数据归你、可带走、顺EU AI Act。/export只导本DO实例(天然按用户隔离),不含任何密钥。
未做(如实):多租户默认开=不可逆线上路由翻转,保持默认关(#41-44本就默认关设计),不擅自翻;
软著/截图不造假。第三枪三块里数据主权这块完整闭环上线,183项全绿。
