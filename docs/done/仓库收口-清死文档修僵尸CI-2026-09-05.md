# TODO — 仓库收口：清死文档 / 修僵尸 CI（nexus-do 后端已删）

> 背景：2026-09-05 iOS 转纯客户端时删掉了 `web/nexus-do/` 后端，但一批 live 文档/CI/工具仍把它当唯一部署主体。
> 分支 `cursor/repo-docs-hygiene-96d7`（off main）。只动 live/权威文件与 CI，历史记录(CHANGELOG/docs/plan/docs/design/shuyu/TODO)不动。

## A. 修/删僵尸 CI（有两个活 bug：改 shuyu 会触发失败）
- [x] A1. 删 `deploy-nexus.yml` —— 触发于 shuyu/**，job `cd web/nexus-do` 必失败；且无 worker 可部署
- [x] A2. 删 `nexus-do-ci.yml` —— 只触发于已删的 web/nexus-do/**，永不运行
- [x] A3. 删 `exec-shell.yml` —— 神枢执行脑通道，dispatcher 随后端删除；顺带收掉任意命令执行面（PR 里点名待权哥可否）
- [x] A4. 修 `shuyu-ci.yml` —— 去 web/nexus-do 触发路径；删会崩的 `sync-check` 作业 → 剩 node-tests + python-tests

## B. 删过时工具 + 清引用
- [x] B1. 删 `tools/check-sync.mjs` `tools/sync-ui.mjs` 以及**多出来的一份** `shuyu/tools/check-sync.mjs`（三份全是 nexus-do 消费副本比对器，现全部找不到对方直接退 1）
- [x] B2. `shuyu/package.json` 去 `check-sync` 脚本
- [x] B3. `Makefile` 重写为 shuyu-only（`make test` = Node + Python），注明 iOS 走 Xcode/CI

## C. 改 live 权威文档为现状
- [x] C1. `CLAUDE.md`（根）：架构现状/常用命令/设计系统铁律(→Theme.swift)/安全红线(→NexusPermissionGate) 全部改为现状，旧 index.html 令牌与 nexus_do.core.mjs 闸门整段标历史遗物
- [x] C2. `STRUCTURE.md`：对照实际文件重写，去 5 个不存在路径，CI 表 5→2
- [x] C3. `README.md`：去死 badge、加架构现状横幅、目录树/构建/部署段改 iOS、Agent Studio 后端条目标历史
- [x] C4. `shuyu/CLAUDE.md` + `shuyu/README.md`：去「消费副本」与 check-sync 表述

## D. 验证 + 提交
- [x] D1. `make test` → Node 84/84、Python 33 OK；两条工作流 YAML 解析通过；README 新链接目标（SUBMIT_GUIDE.md / codemagic.yaml / Bundle ID）核实存在
- [x] D2. 两个逻辑提交（CI+工具 / 文档）+ 推送 + 建 PR（draft），归档本文件

## 结论（2026-09-05 完成）

**起点**：后端删掉后，仓库对新来的 agent 是"谎话连篇"的——`CLAUDE.md`/`README`/`STRUCTURE` 三份权威文档
仍把 `web/nexus-do/` 写成唯一部署主体、常用命令一条都跑不了、设计铁律讲的是不存在的 `index.html`；
并且有**两个活 bug**：改一下 `shuyu/` 就会触发 `deploy-nexus.yml`（`cd web/nexus-do` 失败）和
`shuyu-ci.yml` 的 `sync-check`（`check-sync.mjs` 找不到消费副本退 1），枢语 CI 永远红。

**做了什么**：删 3 条僵尸工作流、修 1 条；删 3 份同源的 nexus-do 消费副本比对脚本（比预想多出一份藏在 `shuyu/tools/`）；
Makefile/package.json 清引用；5 份 live 文档改为「iOS 纯客户端 + shuyu 唯一源」的现状，凡涉后端的段落
不删除而是**明确标为历史遗物并写明可从 git 找回**，防止再有人当现状去对齐。

**刻意没动**：`CHANGELOG.md`、`docs/plan/*`、`docs/design/*`、`shuyu/TODO.md` 是带日期的历史记录，不改写历史；
`web/` 下孤立静态壳资产（sw.js/manifest/图标）留给权哥拍板要不要归档。

**需权哥确认一项**：`exec-shell.yml` 是后端执行脑的任意命令执行通道，dispatcher 已随后端删除、无人再调用；
我把它一并删了（减少攻击面），若仍想保留请说一声，恢复只需 revert 一个文件。
