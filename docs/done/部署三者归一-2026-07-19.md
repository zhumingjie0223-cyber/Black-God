# TODO · 部署三者归一(aquan.lufei.uk + blackgod88 + nexus-do → 一套)

> 权哥:三者合并到仓库,现在没有用户(记忆无包袱,可放手统一)。
> 目标:CF Git 集成直接部署完整大脑到 blackgod88,消除"空壳 blackgod88 vs 大脑 nexus-do"分裂。
> 安全:改根 wrangler 只影响 blackgod88,aquan.lufei.uk(nexus-do)不受影响,最坏构建失败不伤真身。

- [x] 1. 确认大脑 worker 是否自带 studio 路由(合并后别丢工作台)。
- [x] 2. 根 wrangler.jsonc:空壳 assets → 完整大脑(name blackgod88, main 指向已构建的 nexus_do.mjs,
      DO SHENSHU/ShenshuCore + migrations, AI, KV, cron, vars, account_id)。
- [x] 3. 保证 nexus_do.mjs 已最新构建并提交(CF 直接 deploy 提交产物,不必额外 build)。
- [x] 4. 写《部署统一说明 + CF 控制台步骤》(域名 aquan/blackgod.lufei.uk 绑到 blackgod88、废弃冗余)。
- [ ] 5. 提交推送 → CF Git 集成自动部署 → curl 实测 blackgod88 是否变成完整大脑(assetlinks 200/manifest 神枢)。
- [ ] 6. 汇报;TODO 归档 docs/done。CF 控制台的域名绑定/删冗余 worker 由权哥点或授权。

## 进度记录
- 现状:根 wrangler=blackgod88 静态壳(assets:web);web/nexus-do/wrangler=nexus-do 大脑(DO在此,无migrations块)。

## 总结
部署三者归一(权哥确认无用户,放手统一):根 wrangler.jsonc 从"assets:web 静态空壳"改为
"完整大脑"——main 指向已构建提交的 web/nexus-do/nexus_do.mjs,带 DO SHENSHU/ShenshuCore
+migrations v1、AI、KV、cron、vars、account_id。CF Git 集成在根 wrangler deploy 即把完整
大脑部署成 blackgod88(自包含页面/图标/manifest/assetlinks,无需CF再构建)。爆炸半径安全:
只影响 blackgod88,aquan.lufei.uk(nexus-do)不受影响。docs/上架/部署统一说明.md 给了CF控制台
步骤(构建命令/secrets/域名绑定/废弃冗余)。合并后需 curl 实测 blackgod88 是否变完整大脑。
