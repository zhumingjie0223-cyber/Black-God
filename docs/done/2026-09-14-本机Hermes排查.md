# 本机 Hermes 排查（2026-09-14）

目标：看清本机 `~/.hermes` 为什么「感觉不行」。只诊断，未改配置、未换密钥。

## 现状

- 版本：Hermes Agent v0.21.2，装在 `~/.hermes/hermes-agent`，命令 `~/.local/bin/hermes`
- 默认模型：`deepseek-v4-pro`（直连 DeepSeek）
- 电报：已连接，主号「小黄鸭」
- 网关进程在跑，但本机 macOS 的 launchd 管不了它，崩溃不会自动拉起

## 真正不行的点

1. **电报主会话胀死还压不下去**
   - 会话 `20260913_110129_bcfdd3b3` 从 9 月 13 日开到现在
   - 693 条消息、237 次工具调用、一轮要喂约 36 万字
   - 压缩反复失败（DeepSeek 拒收 Hermes 那种压缩请求）
   - 21:04 权哥说「开个新会话」，它没开新的，还在旧会话里回

2. **思考开到最高档**
   - `agent.reasoning_effort: max`
   - 这条会话已累计约 14 万思考字
   - 服务器那次关过思考，本机从 9 月 12 日 `deepseek-tuned` 备份起一直是 `max`

3. **人格和用途对不上**
   - 人格 `professional_cn` 写的是编程 + 授权范围内的红队
   - 实际在用：炒币学习、装技能、管机器人、电报手机遥控
   - 技能装了 80 多个，模型自己也说做不好

4. **桌面语音把环境音当指令**
   - 语音识别开着，语言还是英语
   - 20:48 桌面会话标题就是一串笑声，随后 DeepSeek 报余额不足

5. **备用通道是空的**
   - 多智能体（MOA）开着，要走 OpenRouter + Claude 做裁判，但 OpenRouter 密钥为空，一直报支付错误
   - 没有配置失败后的备用模型
   - 19:42 权哥让换 DeepSeek 密钥，`.env` 没换；21:15 又说「不换了」

6. **Matrix 在空转刷日志**
   - 令牌失效，还在连本机 `127.0.0.1:6167`
   - `errors.log` 被每 5 秒刷一次

## 结论

不是安装坏了，是：**旧会话压不垮、思考太重、人格不对口、备用通道没密钥、语音误触发。** 所以又慢又贵又答不到点上。

权哥没点头前未改任何配置。建议下一步（需明确授权）：

1. 电报真正新开会话（旧的归档）
2. 思考从 `max` 降下来
3. 关掉多智能体
4. 桌面关掉语音，或改成中文
5. 关掉 Matrix 空转
