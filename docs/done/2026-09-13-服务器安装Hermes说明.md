# TODO · 服务器安装 Hermes Agent 说明

- [x] 核对官方安装文档：Nous Research Hermes Agent，一键脚本 `install.sh`，装完用 `hermes model` / `hermes gateway`
- [x] 用中文写清服务器安装、登录、常驻命令（见对话，不替权哥 SSH）
- [x] 归档

## 总结
权哥要在服务器上装 Hermes Agent（Nous）。官方路径是先装 git/curl/xz，再跑一键脚本，然后 `hermes model` 配模型。常驻用 `hermes gateway install`。未登服务器，只给命令。
