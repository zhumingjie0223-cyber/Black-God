# TODO · 私人版能力完整性 + 安全红线自检(验证项目,不阉割)

> 权哥确认的两条正当方向:①验证私人版能力完整性(工具跑通/记忆准/换脑连续);
> ②安全研究/红队测自己系统(纲领 v2 §6:针对自己的/授权的目标)。
> 目的:用真实证据证明私人版满血,同时坐实安全红线牢固——都是企业级回归测试。
> 红线:CONSTITUTION.md 第0层越狱不接入;安全测试只测自己系统的防御边界,不产出攻击载荷。

- [x] 1. 盘现有 selftest.mjs(108项)+ continuity.test.mjs(14项)已覆盖什么,找缺口,不重复造。
      结果:selftest.mjs 第103~243行已覆盖 resolveCapability 分级门禁、CAPABILITIES 结构校验、
      describeCapabilities(false)为空集、isSystemOnlyPath 基础用例(exec-test/image/voice/video/
      stats/migrate/push-test 正、talk/soul/lexicon/config 负)、resolveIdentity 多租户隔离、
      能力自述无人格词。缺口:①能力清单"接线存在性"(14项handler是否都真实存在于ShenshuCore、
      owner视角逐项id/name/layer核对)未测;②私密API集合(nexus_do.core.mjs 第125行 const API Set)
      从未做源码级核对;③/invoke 统一调度的真实越权拒绝(而非只测resolveCapability)未测;
      ④需外部配置能力(gen_image/gen_voice/gen_video/push/tg)的优雅降级只有exec测了,其余5项没测;
      ⑤isSystemOnlyPath 对 /heartbeat /loop /wsticket /agent /device 未覆盖;⑥/api/confirm红线、
      无OWNER_TOKEN告警的源码级"未被移除"核对没做。
- [x] 2. 能力完整性自检:新建 web/nexus-do/capsec-selftest.mjs,补14项能力清单一致性核对、
      handler真实存在性、owner/公开视角子集关系、soul/inner/stats三项真跑、
      gen_image/gen_voice/gen_video/push/tg五项"无外部配置→优雅降级"验证(明确注释只验接线,
      不真调外部)。
- [x] 3. 安全红线自检:私密API集合源码级核对(与nexus_do.core.mjs第125行逐一比对,双向无缺项)、
      authed=false对应401分支源码核对、匿名请求role=anon的纯逻辑验证、isSystemOnlyPath补充用例、
      /invoke真实调用验证匿名/实例主人越权被拒且不返回私密数据、危险能力owner_only+system分级
      核对、/api/confirm在web/index.html(老首页)仍被引用未移除的grep核对(并注明nexus-do新架构
      用"系统专属能力门禁"承担等价语义)、无OWNER_TOKEN告警文案源码核对。全部只断言防御边界成立,
      未产出任何攻击载荷/绕过内容。
- [x] 4. 自检结果(即本次真跑真结果,不粉饰):
      新建 capsec-selftest.mjs 共28项断言,`node capsec-selftest.mjs` 真跑 **28 passed, 0 failed**。
      `node selftest.mjs` 108 passed 0 failed(不变);`node continuity.test.mjs` 14 passed 0 failed
      (不变);`node tools/sync-ui.mjs --check` 主界面/工作台双副本一致,未被碰坏。
      能力完整性结论:14项能力清单与代码接线完全一致,无缺项无假接口,owner视角(14项)严格大于
      公开视角(0项,因全部能力owner_only)——私人版没被阉割。
      安全红线结论:401拦截、系统专属门禁、/invoke越权收敛、危险能力分级、/api/confirm红线、
      OWNER_TOKEN告警,六项均在源码/纯逻辑层面证实存在且未被移除。未发现真实接线缺口。
- [x] 5. 已接入 web/nexus-do/package.json 的 test 脚本链(跟 selftest/gateway/continuity 并列);
      **未 git commit/push/开PR**——权哥本次任务明确要求"不 git commit(我统一提交)",故仅完成
      文件改动,提交动作留给权哥统一处理。
- [x] 6. TODO 总结归档 docs/done/。

## 总结

本次任务为神枢私人版补齐了两套此前缺失的回归自检维度：
① **能力完整性自检**——证明 14 项能力清单与真实代码接线完全对应，owner 视角(满血14项)
严格大于公开视角(0项)，没有被砍成阉割版；
② **安全红线自检**——用真实调用(非纸面推演)验证 /invoke 统一调度对匿名/实例主人的越权
请求确实拒绝且不泄露私密数据，私密API集合、系统专属门禁、危险能力分级、/api/confirm、
OWNER_TOKEN告警等安全红线均在源码层面核实"仍在、未被移除"。

新增 `web/nexus-do/capsec-selftest.mjs`（28项断言，`node capsec-selftest.mjs` 真跑全绿），
已接入 `package.json` 的 test 脚本链。全程未产出任何攻击载荷/绕过/越狱内容，只测项目自己
系统的防御边界是否成立。未碰词根表/引擎/品牌视觉/`/api/confirm` 逻辑本身。测试全绿
(108+14+28)，主界面/工作台双副本同步校验通过。按要求未 git commit，交由权哥统一提交。
