# 本机 Hermes 调整（2026-09-14）

授权：权哥说「你把我给调整好」。密钥不换，人格先不动。

回滚备份：`~/.hermes/backups/config/config.yaml.pre-adjust-20260914-2134`

## 改了什么

| 项 | 之前 | 现在 |
|---|---|---|
| 思考 | `max` | `none`（关掉） |
| 多智能体 | 开着，但 OpenRouter 没密钥 | 关掉 |
| 压缩引擎 | `snapcompact`（DeepSeek 压失败） | 内置 `compressor`，辅助模型 `deepseek-flash` |
| 桌面语音 / 唤醒词 | 开着，语言英语 | 关掉 |
| Matrix | 连已死的本机 6167，每 5 秒刷日志 | `enabled: false`，网关只剩电报 |
| 电报私聊 | 9月13日那条 693 条消息 | 新会话，旧的归档未删 |
| 电报群 | 80 条、约 32 万字 | 同步新开 |

## 核对

- 网关已重启，电报已连接
- 日志：`Gateway running with 1 platform(s)`，不再连 Matrix
- 已向电报主号发过说明

## 用法

电报里直接接着聊。换话题打 `/new`。要压上下文打 `/compress`。
