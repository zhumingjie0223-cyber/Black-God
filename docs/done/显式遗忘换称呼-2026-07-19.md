# TODO — 记忆深化:显式遗忘 + 换称呼(能记·能改·能删)(权哥 2026-07-19)

方向:显式事实记忆上线后(#74),记忆只增不减是残缺——主人说"别叫我那个了/忘掉X",
或给了新称呼,旧的必须让位。补上「改」与「删」,记忆才闭环。纯逻辑,向后兼容。

## 步骤
- [x] extractFacts 加否定守卫:「别/不用叫我X」不再误抓成新称呼;「以后别叫我/忘掉」不误存。
- [x] extractForgets(text):抓「别叫我…」(清称呼)与「忘掉/删掉 X」(按关键词删),普通句不误删。
- [x] applyFactMemory(soul,text):抓新事实 + 换称呼(最新为准,旧称呼让位) + 显式遗忘,去重封顶 50。
- [x] 临界段改调 applyFactMemory(替换原直接 push)。
- [x] selftest +7 用例(否定不误抓/忘掉抓词/普通不误删/换称呼/别叫我清称呼/忘掉删事实/封顶)。
- [x] build + selftest(181 全过) + node --check + sync-ui --check 双副本一致。
- [ ] 中文提交 → 力推分支 → 建草稿 PR → 合并自动部署。

## 红线
- 显式遗忘保守触发(只按关键词删自身小列表,不误伤);不碰 /api/confirm、词根表、身份归一化。

---

## 总结(已完成并合并 #75)
显式遗忘/换称呼上线:extractForgets + applyFactMemory 收口,否定守卫防误抓,自测 181 全过,Cloudflare 已部署。记忆闭环(能记·能改·能删)。
