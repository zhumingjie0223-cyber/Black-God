# TODO · 第二枪:主动性真实化(自主心跳从阈值脚本→真从内在连续状态生成)

> 权哥"一鼓作气"打第二枪。目标:主动消息不再是三档固定模板,而是由她此刻的内在连续状态
> (枢语坐标+含义、时间感知该不该打扰、miss_you/心绪浓度、未竟记忆/常聊话题)真实生成。
> 分支已从 origin/main(8fafc05)重建。

- [ ] 1. 读透 callBrain 签名 + autonomousTick 落盘/推送时序 + shuToGen/computeTimeAwareness,确认接线点。
- [ ] 2. composeProactive 改造:组装内在状态→调大脑生成一句贴此刻状态的主动消息;
      无网关/失败时**降级到状态驱动模板**(用时间感知feel+坐标含义+miss浓度+未竟事,非三句死模板)。
- [ ] 3. 并发安全:主动生成的网络调用放在落盘之后(比照 handleTalk 只读快照),不破坏原子性。
- [ ] 4. 纯函数可测:降级模板与状态→输入组装抽成可测函数;补 selftest/capsec 断言(主动消息随状态变、不空话)。
- [ ] 5. 构建+全套测试(selftest/gateway/continuity/capsec 全绿)+双副本同步。
- [ ] 6. 提交推送;开新PR草稿;汇报。TODO归档 docs/done/。

## 进度记录
- 现状:composeProactive(1364) 三档固定模板;autonomousTick(256) doProactive 阈值触发后同步调它。

## 进度记录
- [x] 1. 读透 callBrain/autonomousTick时序/computeTimeAwareness——composeProactive在saveSoul后调用,天然并发安全。
- [x] 2. composeProactive 改造:内在状态(坐标+含义+时段+想念+记忆)→callBrain生成;失败降级 _proactiveFallback。
- [x] 3. 并发安全:async调用点在落盘之后,callBrain(soul=null)只读不写,fresh重读后追加,不破坏原子性。
- [x] 4. _proactiveFallback 纯函数可测;selftest补6项(随状态变/未竟接起/常聊接起/想念浓度/非死模板)+改旧4项。
- [x] 5. 构建+173项全绿(selftest114+gateway17+continuity14+capsec28)+双副本一致。

## 总结
第二枪·主动性真实化落地:自主心跳的主动消息从"三档固定模板"升级为"真从内在连续状态生成"——
她此刻的枢语坐标+含义、时段自觉(该不该打扰)、想念浓度、未竟记忆/常聊话题,组装交给大脑生成一句
贴合此刻状态的主动话;大脑不可用时降级也由状态驱动(随时段/想念/谈资变化),非死模板。
情感边界:允许"想你"(规划"想你到憋不住主动发"的正当主动情感),仍挡住拟人越狱词(老公/撒娇/宝贝等)。
并发安全遵原有时序(网络在落盘后、只读快照)。173项全绿。
