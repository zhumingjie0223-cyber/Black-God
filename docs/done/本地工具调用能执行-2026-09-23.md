# 检查本地助手工具调用（2026-09-23）

权哥说：现在这个本地的 AI，做事时工具调用不行。先查清断在哪，再改。

- [x] 1. 摸清工具调用链路：规划走流式文本，做事走原生工具回合 — 流式一见工具调用就拒绝；原生只认 `finish_reason=tool_calls` 且参数必须是字符串字典
- [x] 2. 对照测试定位本地兼容接口被拒的点 — 本地模型常见 `stop`、缺结束原因、参数是对象或数字，于是整段被拒，工具不执行
- [x] 3. 修好完整工具调用仍不执行的问题，补测试 — 数字/对象/正文 JSON/流式片段都能执行；半截、截断、内容过滤仍拒绝
- [x] 4. 写结论、提交、开 draft PR — 见文末

## 本轮总结

本地兼容接口的工具调用以前会被整段丢掉：结束原因写成 stop、没写结束原因、参数是对象或数字、调用写在正文或流式片段里，都不执行。现在这些完整调用会真正跑工具。参数没拼完、输出被长度截断、内容过滤，仍然不执行。权限闸没动。

本环境没有 iOS 模拟器，单测还没在模拟器上跑。

## 红线

不部署。不拆危险确认。参数不完整、长度截断、内容过滤，仍然不执行。

## 红线

不部署。不拆危险确认。参数不完整、长度截断、内容过滤，仍然不执行。
