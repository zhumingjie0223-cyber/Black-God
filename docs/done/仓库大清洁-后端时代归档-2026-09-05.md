# TODO — 仓库大清洁（权哥：仓库很不干净）

> 起点：381 文件 / 11MB，docs/ 占 245 文件（64%）；web/ android/ assets/sihan/ 是已删后端与旧人物时代遗留；
> .gitignore 半数规则指向不存在目录。铁律：先归档再删、不 rm -rf 一把梭、不 git add -A。
> 叠在 PR #111（仓库收口）分支上，一个 PR 讲完。

- [x] 1. 并行派 4 路便宜 agent 只读体检 —— docs / 顶层杂项（GPT-5.6 低档），ios-app / shuyu（Gemini Flash 低档）。
  可用清单里没有开源/mini 模型，已如实告知。四路结论：live 区约 60% 是后端时代遗物
- [x] 2. 汇总裁决 —— 明确安全的直接做；sihan 形象 / android 材料 / shuyu.html 演示页涉及资产取舍，列问题不擅动
- [x] 3. 执行安全项 —— 44 处 git mv 归档、6 处去重删除（均 md5 核实）、.gitignore 去 16 条死规则、
  CONTRIBUTING 重写、INDEX 并入 README、模板/工作流注释/CHANGELOG/目录树同步
- [x] 4. 验证 —— live 文档死链 0（自写 /tmp 脚本扫 37 份）、make test 全绿（Node 84 / Python 33）、YAML 解析通过；
  提交 + 推送 + 更新 PR #111 + 归档本文件

## 结论（2026-09-05 完成）

**权哥的直觉是对的。** 四路体检下来，仓库 live 区约六成是 2026-09-05 删掉的 Cloudflare 后端留下的：
`docs/` 245 份里现行只有二十来份，`web/` `skills/` `ui-spec/` `tools/push_to_github.sh` `.env.example` 全部零引用，
`.gitignore` 16 条规则指向不存在的目录，还有 2 对逐字相同的文档、3 张 md5 相同的图标、1 份重复 skill，
`CONTRIBUTING.md` 甚至是从别的项目（uumingtian-max/blackgod）套来的模板。

**做法**：只搬不删。后端时代的规划/架构/部署/上架文档与零引用目录整体 `git mv` 进
`docs/archive/nexus-do后端时代-2026-09-05/`，git 历史完整；去重只删 md5 逐一核实相同的副本。
结果：live 顶层目录 9 → 5，docs 现行文档 53 → 23，live 文档死链 0。

**iOS 那路的发现大部分已在 PR #110 修掉**（888 文案、NSFW 页、四份模型列表、Cloudflare 文案）。**还没修、值得下一轮 iOS 做的**：
商店描述写「一键清除全部数据」但代码里没有这个按钮；截图 4 展示的「历史记录」页代码里不存在；
描述说「金色点缀」而主题是翡翠绿；`NexusRuntime` 与 `NexusExecutor` 两套执行循环并存、`ShuyuIntent` 空架子零引用。

**待权哥拍板（我没动）**：
1. `assets/sihan/`（旧人物形象 3 个文件，~1.1MB）——品牌已去人物化、代码零引用，归档还是保留？
2. `android/`（TWA 上架材料 7 个文件，~2.6MB 截图）——指向已删的网页版域名，现在不能独立工作，归档还是留作将来重建？
3. `shuyu/shuyu.html`——演示页写的还是 29.5 亿/五回路（现在是 76.7 亿/六回路），更新还是删？
4. `docs/archive/` 现在 167 份——git 历史本身就是归档，要不要把 archive 整个清出工作树（只留一个 README 指向历史 commit）？
