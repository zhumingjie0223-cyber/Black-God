# 新版真机安装与启动检查（2026-09-24）

承接黑绿界面与Shell直播重构。用户表示手机已连接，并按iOS要求开启开发者模式、重启并解锁。

- [x] 识别已连接的iPhone 17 Pro Max，iOS 26.7，完成信任与开发模式核实。
- [x] 使用现有团队8429ZL8NQ9、原Bundle ID com.blackgod.nexus构建Apple Development签名版本；自动注册此手机并生成包含此设备的描述文件。
- [x] 定向干净构建通过，codesign严格完整性检查通过。一次增量构建只更新描述文件/资源而遗漏重签，安装前检查发现并通过干净构建消除，没有安装不完整签名包。
- [x] 覆盖安装成功，没有卸载原应用；安装前应用为App Store版本1.2.1(10)，开发包仍使用1.2.1(10)。这是设备本地测试安装，不是商店更新。
- [x] 安装后首次启动前，确认原有聊天、检查点、自我状态、评估、演练文件和BlackGodLinux目录仍在；五个状态文件保存至本机私有恢复目录。
- [x] 启动成功，原生截图确认四页导航和新版会话界面，原有聊天可见。未完成任务显示可继续，没有自动恢复执行。
- [x] 再次读取聊天文件并比较，启动前后字节完全一致，10956字节；启动后查询进程仍在运行。

证据目录：`/Users/aquan/Backups/blackgod-industrial-20260924/validation`。

- 构建与签名：device-clean-build.log，DeviceData/Build/Products/Debug-iphoneos/BlackGod.app。
- 安装前后应用：device-app-before.json、device-app-after.json；device-install.json。
- 启动与进程：device-launch.json、device-process-check.json。
- 原生截图：device-first-launch.png（包含用户自己的历史聊天，不用于商店截图或公开发布）。
- 聊天一致性：device-conversation-integrity.json。
- 本机恢复副本：device-user-data-prelaunch，目录权限0700；不提交Git、不上传。它是安装后首次启动前的五个状态文件副本，不是安装前全应用或钥匙串备份，不包含Linux工作区。

本次实际完成真机安装、启动和聊天数据保留检查。没有在用户手机运行会清空记录或修改权限的完整UI测试，没有自动续跑旧任务或发起真实模型请求。Shell流程此前已在模拟器实际执行；本次未在真机验收Shell完整流程、模型联网、长跑、耗能及磁盘压力。相关发布门槛继续保留。
